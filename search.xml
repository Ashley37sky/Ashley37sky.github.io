<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>arduino-code</title>
    <url>/2020/06/27/arduino-code/</url>
    <content><![CDATA[<h1 id="编程基础"><a href="#编程基础" class="headerlink" title="编程基础"></a>编程基础</h1><h2 id="数字I-O"><a href="#数字I-O" class="headerlink" title="数字I/O"></a>数字I/O</h2><h3 id="数字信号"><a href="#数字信号" class="headerlink" title="数字信号"></a>数字信号</h3><a id="more"></a>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在大多数Arduino控制板上 13号引脚都连接了一个标有“L”的LED灯</span></span><br><span class="line"><span class="keyword">int</span> led = <span class="number">13</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span></span>&#123;</span><br><span class="line">  <span class="built_in">pinMode</span>(led, <span class="literal">OUTPUT</span>);     </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="built_in">digitalWrite</span>(led, <span class="literal">HIGH</span>);   <span class="comment">// 点亮LED</span></span><br><span class="line">  <span class="built_in">delay</span>(<span class="number">1000</span>);           <span class="comment">// 等待一秒钟</span></span><br><span class="line">  <span class="built_in">digitalWrite</span>(led, <span class="literal">LOW</span>);   <span class="comment">// 通过将引脚电平拉低，关闭LED</span></span><br><span class="line">  <span class="built_in">delay</span>(<span class="number">1000</span>);           <span class="comment">// 等待一秒钟</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="流水灯实验"><a href="#流水灯实验" class="headerlink" title="流水灯实验"></a>流水灯实验</h3><p><img src="https://www.arduino.cn/data/attachment/forum/201801/20/211432ahrihu0suh111hrh.png" alt="img"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">2</span>;i&lt;<span class="number">8</span>;i++)</span><br><span class="line">    <span class="built_in">pinMode</span>(i,<span class="literal">OUTPUT</span>);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="comment">// 从引脚2到引脚6，逐个点亮LED，等待1秒再熄灭LED</span></span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">2</span>;i&lt;<span class="number">7</span>;i++)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="built_in">digitalWrite</span>(i,<span class="literal">HIGH</span>);</span><br><span class="line">    <span class="built_in">delay</span>(<span class="number">1000</span>);</span><br><span class="line">    <span class="built_in">digitalWrite</span>(i,<span class="literal">LOW</span>);   </span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 从引脚7到引脚3，逐个点亮LED，等待1秒再熄灭LED</span></span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">7</span>;i&gt;<span class="number">2</span>;i--)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="built_in">digitalWrite</span>(i,<span class="literal">HIGH</span>);</span><br><span class="line">    <span class="built_in">delay</span>(<span class="number">1000</span>);</span><br><span class="line">    <span class="built_in">digitalWrite</span>(i,<span class="literal">LOW</span>);   </span><br><span class="line">  &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="按键控制LED实验"><a href="#按键控制LED实验" class="headerlink" title="按键控制LED实验"></a>按键控制LED实验</h3><p><img src="https://www.arduino.cn/data/attachment/forum/201801/20/212234wfsxa702afgaamad.png" alt=""></p>
<p><strong>限流电阻</strong><br>一般LED的最大能承受的电流为25mA，如若直接将LED连接到电路中，当其点亮时，如果电流过大，很容易烧毁。如图2-24所示，我们在LED一端串联了一个电阻R2，这样做可以控制流过LED的电流，防止损坏LED。这个电阻我们称之为限流电阻。</p>
<p><strong>下拉电阻</strong><br>在2号引脚到GND之前，连接了一个阻值10K的电阻。如果没有该电阻，当未按下按键时，2号引脚会一直处于悬空 状态，此时使用digital Read() 读取2号引脚状态，会得到一个不稳定的值（可能是高，也可能是低）。添加这个R1电阻到地就是为了稳定引脚的电平，当引脚悬空时，就会识别为低电平。而这种将某节点通过电阻接地的做法，叫做下拉，这个电阻叫做下拉电阻。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> buttonPin = <span class="number">2</span>;     <span class="comment">// 连接按键的引脚</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> ledPin =  <span class="number">13</span>;      <span class="comment">// 连接LED的引脚</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">int</span> buttonState = <span class="number">0</span>;         <span class="comment">// 存储按键状态的变量</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">pinMode</span>(ledPin, <span class="literal">OUTPUT</span>);      </span><br><span class="line">  <span class="built_in">pinMode</span>(buttonPin, <span class="literal">INPUT</span>);     </span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span></span>&#123;</span><br><span class="line">  <span class="comment">// 读取按键状态并存储在变量中</span></span><br><span class="line">  buttonState = <span class="built_in">digitalRead</span>(buttonPin);</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// 检查按键是否被按下</span></span><br><span class="line">  <span class="comment">// 如果按键按下，那buttonState应该为高电平</span></span><br><span class="line">  <span class="keyword">if</span> (buttonState == <span class="literal">HIGH</span>) &#123;     </span><br><span class="line">    <span class="comment">// 点亮LED</span></span><br><span class="line">    <span class="built_in">digitalWrite</span>(ledPin, <span class="literal">HIGH</span>);  </span><br><span class="line">  &#125; </span><br><span class="line">  <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 熄灭LED</span></span><br><span class="line">    <span class="built_in">digitalWrite</span>(ledPin, <span class="literal">LOW</span>); </span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="进阶（上拉电阻）"><a href="#进阶（上拉电阻）" class="headerlink" title="进阶（上拉电阻）"></a>进阶（上拉电阻）</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201801/20/212701dvu4tmkujk3lvsdx.png" alt="img"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> buttonPin = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">int</span> ledPin = <span class="number">13</span>;</span><br><span class="line"><span class="keyword">int</span> buttonState = <span class="number">0</span>; </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="built_in">pinMode</span>(buttonPin,<span class="literal">INPUT_PULLUP</span>);</span><br><span class="line">  <span class="built_in">pinMode</span>(ledPin,<span class="literal">OUTPUT</span>);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  buttonState = <span class="built_in">digitalRead</span>(buttonPin);</span><br><span class="line">  <span class="comment">// 按住按键时，点亮LED；放开按键后，熄灭LED。</span></span><br><span class="line">  <span class="keyword">if</span>(buttonState==<span class="literal">HIGH</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="built_in">digitalWrite</span>(ledPin,<span class="literal">LOW</span>);   <span class="comment">//上拉电阻</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="built_in">digitalWrite</span>(ledPin,<span class="literal">HIGH</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="进阶（按一下亮，按一下熄灭）"><a href="#进阶（按一下亮，按一下熄灭）" class="headerlink" title="进阶（按一下亮，按一下熄灭）"></a>进阶（按一下亮，按一下熄灭）</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> buttonPin = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">int</span> ledPin = <span class="number">13</span>;</span><br><span class="line"><span class="keyword">boolean</span> ledState=<span class="literal">false</span>;  </span><br><span class="line"><span class="keyword">boolean</span> buttonState=<span class="literal">true</span>;  </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="built_in">pinMode</span>(buttonPin, <span class="literal">INPUT_PULLUP</span>);</span><br><span class="line">  <span class="built_in">pinMode</span>(ledPin,<span class="literal">OUTPUT</span>);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="comment">// 等待按键按下</span></span><br><span class="line"><span class="keyword">while</span>(<span class="built_in">digitalRead</span>(buttonPin)==<span class="literal">HIGH</span>)&#123;&#125;</span><br><span class="line"> </span><br><span class="line">  <span class="comment">// 当按键按下时，点亮或熄灭LED</span></span><br><span class="line">  <span class="keyword">if</span>(ledState==<span class="literal">true</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="built_in">digitalWrite</span>(ledPin,<span class="literal">LOW</span>);</span><br><span class="line">    ledState=!ledState;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="built_in">digitalWrite</span>(ledPin,<span class="literal">HIGH</span>);</span><br><span class="line">    ledState=!ledState;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">delay</span>(<span class="number">500</span>);  <span class="comment">// 很重要</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>程序末尾有一个<code>delay(500)</code> 的延时，它在这里是极其<strong>重要</strong>的。删去这个延时操作，按键出现失灵。这是因为程序运行的非常快，没有了延时操作，你按下按键到放开按键的间隔时间虽然极短，但loop中的语句可能已经运行了很多次，很难确定你放开按键时正在运行的loop() 循环是点亮还是熄灭LED。我们使用延时操作来使两次按键间产生一定的间隔时间，在间隔时间内Arduino会忽略按键按下情况，从而达到<strong>区分两次按键</strong>的目的。</p>
<h2 id="模拟I-O的使用"><a href="#模拟I-O的使用" class="headerlink" title="模拟I/O的使用"></a>模拟I/O的使用</h2><h3 id="呼吸灯实验"><a href="#呼吸灯实验" class="headerlink" title="呼吸灯实验"></a>呼吸灯实验</h3><p><img src="https://www.arduino.cn/data/attachment/forum/201803/05/210943ipvv9qj2tvdxxqpk.png" alt="img"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">nt led = <span class="number">9</span>;           <span class="comment">// LED灯连接在9号引脚</span></span><br><span class="line"><span class="keyword">int</span> brightness = <span class="number">0</span>;     <span class="comment">// LED灯亮度</span></span><br><span class="line"><span class="keyword">int</span> fadeAmount = <span class="number">5</span>;   <span class="comment">// 亮度渐变值</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">pinMode</span>(led, <span class="literal">OUTPUT</span>);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">analogWrite</span>(led, brightness);</span><br><span class="line">  brightness = brightness + fadeAmount;</span><br><span class="line">  <span class="keyword">if</span> (brightness == <span class="number">0</span> || brightness == <span class="number">255</span>) &#123;</span><br><span class="line">    fadeAmount = -fadeAmount ;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">delay</span>(<span class="number">30</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>delay(30) 的延时语句，这是为了让我们肉眼能观察到亮度调节的效果。如果没有这个语句，整个变化效果将一闪而过。</p>
<h4 id="进阶（控制呼吸灯的呼吸频率）"><a href="#进阶（控制呼吸灯的呼吸频率）" class="headerlink" title="进阶（控制呼吸灯的呼吸频率）"></a>进阶（控制呼吸灯的呼吸频率）</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201803/05/210944d0b0izhh002bbnqq.jpg" alt="img"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> ledPin = <span class="number">9</span>;  <span class="comment">// 9号引脚控制LED</span></span><br><span class="line"><span class="keyword">int</span> pot=A0;    <span class="comment">// A0引脚读取电位器输出电压</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span></span>&#123;&#125; </span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span></span>&#123; </span><br><span class="line">  <span class="comment">// LED逐渐变亮</span></span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">int</span> fadeValue = <span class="number">0</span> ; fadeValue &lt;= <span class="number">255</span>; fadeValue +=<span class="number">5</span>) </span><br><span class="line">  &#123; </span><br><span class="line">    <span class="built_in">analogWrite</span>(ledPin, fadeValue);</span><br><span class="line"><span class="comment">// 读取电位器输出电压，除以5时为了缩短延时时间</span></span><br><span class="line"><span class="keyword">int</span> time=<span class="built_in">analogRead</span>(pot)/<span class="number">5</span>;</span><br><span class="line">    <span class="built_in">delay</span>(time);  <span class="comment">// 将time用于延时</span></span><br><span class="line">  &#125; </span><br><span class="line">  <span class="comment">// LED逐渐变暗</span></span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">int</span> fadeValue = <span class="number">255</span> ; fadeValue &gt;= <span class="number">0</span>; fadeValue -=<span class="number">5</span>) </span><br><span class="line">  &#123; </span><br><span class="line">	<span class="built_in">analogWrite</span>(ledPin, fadeValue);</span><br><span class="line">    <span class="built_in">delay</span>(<span class="built_in">analogRead</span>(pot)/<span class="number">5</span>);  <span class="comment">// 读取电位器输出电压，并用于延时</span></span><br><span class="line">  &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="光敏电阻检测环境光实验"><a href="#光敏电阻检测环境光实验" class="headerlink" title="光敏电阻检测环境光实验"></a>光敏电阻检测环境光实验</h3><p><img src="https://www.arduino.cn/data/attachment/forum/201803/05/212143wldvvylf33xqv33b.jpg" alt="img"></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">void setup()</span><br><span class="line">&#123;</span><br><span class="line">  &#x2F;&#x2F; 初始化串口</span><br><span class="line">  Serial.begin(9600);</span><br><span class="line">&#125;</span><br><span class="line">void loop() </span><br><span class="line">&#123;</span><br><span class="line">&#x2F;&#x2F; 读出当前光线强度，并输出到串口显示</span><br><span class="line">  int sensorValue &#x3D; analogRead(A0);</span><br><span class="line">  Serial.println(sensorValue);</span><br><span class="line">  delay(1000);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="https://www.arduino.cn/data/attachment/forum/201803/05/212420xai1n4aicakauqii.jpg" alt="img"></p>
<p>由于电源波动或外界干扰等原因，输出的数据可能也会受到一定的影响，例如波动较大等现象，这时你可以通过读取多次传感器数值，求平均数的方法，减小数据的波动。</p>
<h2 id="串口的使用"><a href="#串口的使用" class="headerlink" title="串口的使用"></a>串口的使用</h2><h4 id="串口输出"><a href="#串口输出" class="headerlink" title="串口输出"></a>串口输出</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int counter&#x3D;0; &#x2F;&#x2F; 计数器</span><br><span class="line"> </span><br><span class="line">void setup() &#123;</span><br><span class="line">&#x2F;&#x2F; 初始化串口</span><br><span class="line">  Serial.begin(9600);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">void loop() &#123;</span><br><span class="line">&#x2F;&#x2F; 每loop循环一次，计数器变量加1</span><br><span class="line">counter &#x3D; counter+1;</span><br><span class="line">&#x2F;&#x2F; 输出变量</span><br><span class="line">Serial.print(counter);</span><br><span class="line">&#x2F;&#x2F; 输出字符</span><br><span class="line">Serial.print( &#39;:&#39; );</span><br><span class="line">&#x2F;&#x2F; 输出字符串;</span><br><span class="line">Serial.println(&quot;Hellow World&quot;);</span><br><span class="line">delay(1000);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="串口输入"><a href="#串口输入" class="headerlink" title="串口输入"></a>串口输入</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">void setup() &#123;</span><br><span class="line">  &#x2F;&#x2F; 初始化串口</span><br><span class="line">  Serial.begin(9600);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">void loop() &#123;</span><br><span class="line">&#x2F;&#x2F; 如果缓冲区中有数据，则读取并输出</span><br><span class="line">if(Serial.available()&gt;0)</span><br><span class="line">  &#123;</span><br><span class="line">    char ch&#x3D;Serial.read();</span><br><span class="line">    Serial.print(ch);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    </p>
]]></content>
      <categories>
        <category>Arduino</category>
      </categories>
      <tags>
        <tag>Arduino</tag>
      </tags>
  </entry>
  <entry>
    <title>Arduino基础</title>
    <url>/2020/06/27/arduino/</url>
    <content><![CDATA[<h1 id="编程入门"><a href="#编程入门" class="headerlink" title="编程入门"></a>编程入门</h1><h2 id="C-C-语言基础"><a href="#C-C-语言基础" class="headerlink" title="C\C++语言基础"></a>C\C++语言基础</h2><a id="more"></a>

<ul>
<li>浮点型数据的运算，速度较慢且可能有精度丢失。通常我们会把浮点型转换为整型来处理相关运算。如9.8cm，我们通常把换算为98mm来计算</li>
<li>存储字符时，字符需要用单引号引用，如<code>char col=’C’;</code> </li>
<li>相较于数组形式的定义方法，使用String类型定义字符串会占用更多的存储空间</li>
<li>Switch后的表达式结果只能是整形或字符型。如果要使用其他类型，则必须使用if语句</li>
</ul>
<h2 id="常用电子元件"><a href="#常用电子元件" class="headerlink" title="常用电子元件"></a>常用电子元件</h2><h4 id="面包板"><a href="#面包板" class="headerlink" title="面包板"></a>面包板</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201704/15/202407hn5028ql688082nn.jpg" alt="面包内部、.jpg"></p>
<h4 id="电阻"><a href="#电阻" class="headerlink" title="电阻"></a>电阻</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201704/15/202658ek379kisa3pi3vzh.jpg" alt="1.jpg"></p>
<p>下拉电阻：将某节点通过电阻接地的做法，叫做下拉，这个电阻叫做下拉电阻</p>
<p>上拉电阻：同下拉电阻一样，可以稳定I/O口电平，不同的是电阻连接到VCC，将引脚稳定在高电位。</p>
<p>​                    可以用<code>pinMode(buttonPin,INPUT_PULLUP)</code>或者物理连接实现</p>
<h4 id="电容"><a href="#电容" class="headerlink" title="电容"></a>电容</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201704/15/202736q9kswuxis5nl825i.jpg" alt="2.jpg"></p>
<p>电容也有很多作用，如去耦、滤波、储能等</p>
<h4 id="二极管"><a href="#二极管" class="headerlink" title="二极管"></a>二极管</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201704/15/202812i9v9k8tly9hwhvxl.jpg" alt="3.jpg"></p>
<p>二极管在电路中使用广泛，作用众多，如整流、稳压、电路保护等</p>
<h4 id="LED（发光二极管"><a href="#LED（发光二极管" class="headerlink" title="LED（发光二极管"></a>LED（发光二极管</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201704/15/202849hnqjq8mqtntmj8zm.jpg" alt="4.jpg"></p>
<p>一般LED的最大能承受的电流为25mA</p>
<h4 id="三极管"><a href="#三极管" class="headerlink" title="三极管"></a>三极管</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201704/15/202929l3to1oz4wtwf6chn.jpg" alt="5.jpg"></p>
<p>三极管，能起放大、开关等作用的元件。<br>有发射极（emitter,E）、基极（base,B）和集电极（collector,C）三级，<br>有pnp和npn 两种类型的三极管。</p>
<h4 id="按键"><a href="#按键" class="headerlink" title="按键"></a>按键</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201801/20/212948cbd22kta69immltw.jpg" alt="img"></p>
<h4 id="电位器"><a href="#电位器" class="headerlink" title="电位器"></a>电位器</h4><p><img src="https://www.arduino.cn/data/attachment/forum/201803/05/210944q3dre8qok1daddvt.png" alt="img"></p>
<p>电位器是一个可调电阻</p>
<h4 id="光敏电阻"><a href="#光敏电阻" class="headerlink" title="光敏电阻"></a>光敏电阻</h4><h4 id=""><a href="#" class="headerlink" title=""></a><img src="https://www.arduino.cn/data/attachment/forum/201803/05/212142at6fw8a8rkx8n6ka.png" alt="img" style="zoom:67%;" /></h4><p>电阻值随照射光强度增加而下降的电阻</p>
<h1 id="编程基础"><a href="#编程基础" class="headerlink" title="编程基础"></a>编程基础</h1><p><img src="https://www.arduino.cn/data/attachment/forum/201801/20/210906pl8cbzbqyeiq8ill.jpg" alt="img"></p>
<h2 id="数字I-O的使用"><a href="#数字I-O的使用" class="headerlink" title="数字I/O的使用"></a>数字I/O的使用</h2><ol>
<li><p>新建变量</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#define LED 13</span><br><span class="line">int led &#x3D; 13;</span><br></pre></td></tr></table></figure>
</li>
<li><p>配置引脚模式</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">pinMode</span>(pin, mode);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>可使用的三种模式:</p>
<ul>
<li><strong>INPUT</strong>     输入模式</li>
<li><strong>OUTPUT</strong>     输出模式</li>
<li><strong>INPUT_PULLUP</strong>     输入上拉模式</li>
</ul>
</blockquote>
</li>
<li><p>输出高低电平</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">digitalWrite(pin, value); &#x2F;&#x2F;value&#x3D; HIGH&#x2F;LOW</span><br></pre></td></tr></table></figure>
</li>
<li><p>读取外部输入的数字信号</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int value &#x3D; digitalRead(pin)</span><br></pre></td></tr></table></figure>

<blockquote>
<p>Arduino UNO会将大于3V的输入电压视为高电平，小于1.5V的电压视为低电平。超过5V的输入电压可能会损坏Arduino UN</p>
</blockquote>
</li>
</ol>
<p><em>在Arduino核心库中，OUTPUT被定义等于1，INPUT被定义等于0，HIGH被定义等于1，LOW被定义等于0，所以可以写成<code>pinMode(13,1);</code> <code>digitalWrite(13,1)</code>。但是不建议，因为可读性变差</em></p>
<ol start="5">
<li><p>延时函数。在写程序的时候要注意写delay()</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">delay() ; &#x2F;&#x2F;ms</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="模拟I-O的使用"><a href="#模拟I-O的使用" class="headerlink" title="模拟I/O的使用"></a>模拟I/O的使用</h2><ol>
<li><p>编号带“A”的引脚是模拟输入引脚。读取引脚上输入的电压大小。Arduino UNO上，可以接受0～5V的模拟信号。Arduino 模拟输入功能有10位精度，即可以将0～5V的电压信号转换为0～1023的整数形式表示</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int value &#x3D; analogRead(pin)</span><br></pre></td></tr></table></figure>
</li>
<li><p>模拟输出功能。以脉冲宽度调制（PWM）来达到输出近似模拟值的效果。这里仅仅是得到了近似模拟值输出的效果，如果要输出真正的模拟值，还需要加上外围滤波电路</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">analogWrite(pin,value)</span><br></pre></td></tr></table></figure>

<p>value指定是PWM的脉冲宽度，范围为0～255。</p>
</li>
</ol>
<h2 id="串口的使用"><a href="#串口的使用" class="headerlink" title="串口的使用"></a>串口的使用</h2><p>Arduino与计算机通信最常用的方式就是串口通信。串口通信时，Arduino控制器上的标有RX/TX的两LED灯会闪烁提示，接收数据时，RX灯会点亮；发送数据时，TX灯会点亮。</p>
<ol>
<li><p>使用串口与计算机通信，需要先使用Serial. Begin() 初始化Arduino的串口通信功能</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Serial.begin(speed);</span><br></pre></td></tr></table></figure>

<p>peed是指串口通信波特率,<strong>波特率</strong>是衡量通信速度的参数，表示每秒钟传送的bit的个数。串口通信的双方必须使用同样的波特率，方能正常进行通信。</p>
</li>
<li><p>串口输出。使用Serial. Print() 或Serial.println() 向计算机发送各种类型的信息</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Serial</span>.<span class="built_in">print</span>(val);</span><br><span class="line"><span class="built_in">Serial</span>.<span class="built_in">println</span>(val);  <span class="comment">//回车换行</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>串口监视器是Arduino IDE自带的一个小工具，可以查看到串口传来的信息，也可以向连接的设备发送信息</p>
</li>
<li><p>缓冲区。在使用串口时，Arduino Uno会在SRAM中开辟一段大小为256 bytes的空间，串口接收到的数据都会被暂时存放进这个空间中，这个存储空间，我们称之为缓冲区。当你调用Serial. Read()语句时，Arduino便会从缓冲区取出一个字节的数据</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Serial</span>.<span class="built_in">available</span>(); <span class="comment">//缓冲区中接收到的数据字节数</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>串口输入</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">erial.<span class="built_in">read</span>(); <span class="comment">//每次都会返回一个字节的数据</span></span><br></pre></td></tr></table></figure>

<p>当缓冲区中没有可读数据时，read()函数会返回int型值-1，而int型-1对应的char型数据是乱码。所以要先判断缓冲区是否有数据。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">if( Serial.available() &gt; 0 ) </span><br><span class="line">while( Serial.available() &gt; 0 )</span><br></pre></td></tr></table></figure>
</li>
<li><p>特殊用法。等待串口监视器开启，开启串口监视器后，!Serial将为假</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">while (!Serial) &#123; &#125;</span><br></pre></td></tr></table></figure>



</li>
</ol>
]]></content>
      <categories>
        <category>Arduino</category>
      </categories>
      <tags>
        <tag>Arduino</tag>
      </tags>
  </entry>
  <entry>
    <title>coursera批量下载</title>
    <url>/2020/07/15/couseradownload/</url>
    <content><![CDATA[<blockquote>
<p><a href="https://github.com/LoganDing/Coursera.org-Downloader" target="_blank" rel="noopener">https://github.com/LoganDing/Coursera.org-Downloader</a></p>
</blockquote>
<h1 id="methods"><a href="#methods" class="headerlink" title="methods"></a>methods</h1><a id="more"></a>

<ol>
<li><p>进入python安装目录下<code>Scripts</code> ，并且将路径添加到系统path中</p>
<p><code>我的电脑-&gt;属性-&gt;高级系统设置-&gt;高级-&gt;环境变量-&gt;系统变量-&gt;path-&gt;新建</code></p>
</li>
<li><p>安装包</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install coursera-dl</span><br></pre></td></tr></table></figure>
</li>
<li><p>运行脚本</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">coursera-dl -u &lt;user&gt; -p &lt;pass&gt; &lt;coursename&gt;</span><br></pre></td></tr></table></figure>

<p><em>course_name一般在URL中learn/course_name</em></p>
</li>
<li><p>若出现无法登录问题，添加cauth</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">coursera-dl -u &lt;user&gt; -p &lt;pass&gt; --cauth &lt;cauth&gt; &lt;coursename&gt;</span><br></pre></td></tr></table></figure>

<p>如何获取cauth？<code>f12-&gt;application-&gt;cookies(https://www.coursera.org/)-&gt;CAUTH</code></p>
</li>
<li><p>其他操作</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Without -p field:            coursera-dl -u &lt;user&gt; modelthinking-004</span><br><span class="line">Multiple classes:            coursera-dl -u &lt;user&gt; -p &lt;pass&gt; saas historyofrock1-001 algo-2012-002</span><br><span class="line">Filter by section name:      coursera-dl -u &lt;user&gt; -p &lt;pass&gt; -sf &quot;Chapter_Four&quot; crypto-004</span><br><span class="line">Filter by lecture name:      coursera-dl -u &lt;user&gt; -p &lt;pass&gt; -lf &quot;3.1_&quot; ml-2012-002</span><br><span class="line">Download only ppt files:     coursera-dl -u &lt;user&gt; -p &lt;pass&gt; -f &quot;ppt&quot; qcomp-2012-001</span><br><span class="line">Use a ~&#x2F;.netrc file:         coursera-dl -n -- matrix-001</span><br><span class="line">Get the preview classes:     coursera-dl -n -b ni-001</span><br><span class="line">Download videos at 720p:     coursera-dl -n --video-resolution 720p ni-001</span><br><span class="line">Specify download path:       coursera-dl -n --path&#x3D;C:\Coursera\Classes\ comnetworks-002</span><br><span class="line">Display help:                coursera-dl --help</span><br><span class="line"></span><br><span class="line">Maintain a list of classes in a dir:</span><br><span class="line">Initialize:              mkdir -p CURRENT&#x2F;&#123;class1,class2,..classN&#125;</span><br><span class="line">Update:                  coursera-dl -n --path CURRENT &#96;\ls CURRENT&#96;</span><br></pre></td></tr></table></figure>

<p>回复下载</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">coursera-dl -u &lt;user&gt; -p &lt;pass&gt; --resume sdn1-001</span><br></pre></td></tr></table></figure>

<p><em><code>CTRL+C</code>, partially downloaded files will be deleted from your disk and you have to start the download process from the beginning, other interruptions can use the restore</em></p>
</li>
</ol>
<h1 id="why？？（填坑）"><a href="#why？？（填坑）" class="headerlink" title="why？？（填坑）"></a>why？？（填坑）</h1><p><code>pip</code>：</p>
<p><code>cookie</code></p>
<p><code>cauth</code></p>
<p><code>f12</code></p>
<p><code>批处理</code></p>
]]></content>
      <tags>
        <tag>hacks</tag>
      </tags>
  </entry>
  <entry>
    <title>conda</title>
    <url>/2020/11/15/conda/</url>
    <content><![CDATA[<h1 id="conda"><a href="#conda" class="headerlink" title="conda"></a>conda</h1><ul>
<li><code>conda update conda</code>升级</li>
</ul>
<h2 id="环境管理"><a href="#环境管理" class="headerlink" title="环境管理"></a>环境管理</h2><ul>
<li><code>conda create --name py34 python=3.4</code>   创建环境</li>
<li><code>source activate py34</code> 激活环境</li>
<li><code>conda info -envis</code> 确认当前环境</li>
</ul>
<a id="more"></a>

<ul>
<li><code>source deactivate</code>   退出环境</li>
<li><code>conda remove -n py34 --all</code> 删除环境</li>
<li><code>conda info -e</code> 查看已有环境</li>
</ul>
<h2 id="包管理"><a href="#包管理" class="headerlink" title="包管理"></a>包管理</h2><ul>
<li><p>镜像</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 添加Anaconda的TUNA镜像</span><br><span class="line">conda config --add channels https:&#x2F;&#x2F;mirrors.tuna.tsinghua.edu.cn&#x2F;anaconda&#x2F;pkgs&#x2F;free&#x2F;</span><br><span class="line">conda config --add channels https:&#x2F;&#x2F;mirrors.tuna.tsinghua.edu.cn&#x2F;anaconda&#x2F;pkgs&#x2F;main&#x2F;</span><br><span class="line">conda config --set show_channel_urls yes</span><br></pre></td></tr></table></figure>



</li>
</ul>
<ul>
<li><p><code>conda list</code>查看在当前环境中，已安装的包和对应版本</p>
</li>
<li><p><code>conda search X</code>     查找可安装的包</p>
</li>
<li><p><code>conda install X</code></p>
</li>
<li><p><code>conda update X</code></p>
</li>
<li><p><code>conda remove X</code></p>
</li>
</ul>
]]></content>
      <tags>
        <tag>conda</tag>
      </tags>
  </entry>
  <entry>
    <title>coursera/github慢</title>
    <url>/2020/07/15/dns_speedup/</url>
    <content><![CDATA[<h1 id="methods"><a href="#methods" class="headerlink" title="methods"></a>methods</h1><a id="more"></a>

<ol>
<li><p>查找域名对应的ip地址</p>
<blockquote>
<p>github:</p>
<ul>
<li><p>github.com</p>
</li>
<li><p>github.global.ssl.fastly.net </p>
</li>
</ul>
</blockquote>
<blockquote>
<p>coursera:</p>
<ul>
<li>d3c33hcgiwev3.cloudfront.net</li>
</ul>
</blockquote>
<p>域名解析网站</p>
<ul>
<li><a href="https://www.ipaddress.com/" target="_blank" rel="noopener">https://www.ipaddress.com/</a></li>
<li><a href="http://ping.chinaz.com/d3c33hcgiwev3.cloudfront.net" target="_blank" rel="noopener">http://ping.chinaz.com/d3c33hcgiwev3.cloudfront.net</a></li>
</ul>
</li>
<li><p>修改hosts <code>C:\Windows\System32\drivers\etc\hosts</code> 文件</p>
<blockquote>
<p><ip> &lt;域名&gt;</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">87.248.214.183 static3.cdn.Ubi.com</span><br><span class="line">95.140.224.199 static2.cdn.Ubi.com</span><br><span class="line">87.248.214.183 static1.cdn.Ubi.com</span><br><span class="line">221.110.130.114 uplaypc-s-ubisoft.cdn.ubi.com</span><br><span class="line">221.110.130.114 uplaypc-s-ubisoft-ww.cdn.ubi.com</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">140.82.113.3 github.com</span><br><span class="line">199.232.69.194  github.global.ssl.fastly.net </span><br><span class="line"></span><br><span class="line">52.84.167.78 d3c33hcgiwev3.cloudfront.net</span><br><span class="line">13.224.164.137 d3c33hcgiwev3.cloudfront.net</span><br><span class="line">13.224.164.148  d3c33hcgiwev3.cloudfront.net</span><br><span class="line">99.84.194.187  d3c33hcgiwev3.cloudfront.net</span><br><span class="line">52.84.186.109  d3c33hcgiwev3.cloudfront.net</span><br><span class="line">54.230.85.123 d3c33hcgiwev3.cloudfront.net</span><br><span class="line">13.224.164.137    d3c33hcgiwev3.cloudfront.net</span><br><span class="line">14.13.224.164.148    d3c33hcgiwev3.cloudfront.net</span><br><span class="line">15.13.224.164.208    d3c33hcgiwev3.cloudfront.net</span><br><span class="line">16.13.224.164.173   d3c33hcgiwev3.cloudfront.net</span><br><span class="line">17.31.13.78.66    d3c33hcgiwev3.cloudfront.net</span><br></pre></td></tr></table></figure>

<p>(混入奇奇怪怪的东西)</p>
</li>
<li><p>cmd中使用<code>ipconfig\flushdns</code></p>
</li>
</ol>
<h1 id="why（填坑？？）"><a href="#why（填坑？？）" class="headerlink" title="why（填坑？？）"></a>why（填坑？？）</h1><p><code>hosts</code>:其作用就是将一些常用的网址域名与其对应的IP地址建立一个关联“数据库”，当用户在浏览器中输入一个需要登录的网址时，系统会首先自动从Hosts文件中寻找对应的IP地址，一旦找到，系统会立即打开对应网页，如果没有找到，则系统会再将网址提交DNS域名解析服务器进行IP地址的解析。<br>需要注意的是，Hosts文件配置的映射是静态的，如果网络上的计算机更改了请及时更新IP地址，否则将不能访问。</p>
<p><code>dns</code>: ????</p>
]]></content>
      <tags>
        <tag>hacks</tag>
      </tags>
  </entry>
  <entry>
    <title>git命令</title>
    <url>/2020/06/20/git-cheatsheet/</url>
    <content><![CDATA[<h1 id="创建"><a href="#创建" class="headerlink" title="创建"></a>创建</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git add &lt;file&gt;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git commit -m &lt;message&gt;</span><br></pre></td></tr></table></figure>

<h1 id="版本回退删除"><a href="#版本回退删除" class="headerlink" title="版本回退删除"></a>版本回退删除</h1><h2 id="查看"><a href="#查看" class="headerlink" title="查看"></a>查看</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git status %查看仓库状态,是否有文件被修改过</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git diff &lt;file&gt; 		  %working tree与index差别</span><br><span class="line">git diff --cached &lt;file&gt;  %index与repository差别</span><br><span class="line">git diff HEAD &lt;file&gt;  	  %查看working tree和repository差别。HEAD代表的是新commit的信息。</span><br></pre></td></tr></table></figure>

<h2 id="版本回退"><a href="#版本回退" class="headerlink" title="版本回退"></a>版本回退</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git log %查看提交日志</span><br><span class="line">git log --pretty&#x3D;oneline</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reset --hard HEAD^       %回退到上一个版本</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reset --hard 1094a      %版本号没必要写全，前几位就可以了，Git会自动去找</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reflog  %查看每一次命令</span><br></pre></td></tr></table></figure>

<h2 id="撤销修改"><a href="#撤销修改" class="headerlink" title="撤销修改"></a>撤销修改</h2><h4 id="撤销工作区"><a href="#撤销工作区" class="headerlink" title="撤销工作区"></a>撤销工作区</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git checkout -- &lt;file&gt;  %把在工作区的修改全部撤销</span><br></pre></td></tr></table></figure>

<h4 id="撤销暂存区"><a href="#撤销暂存区" class="headerlink" title="撤销暂存区"></a>撤销暂存区</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reset HEAD &lt;file&gt; %把暂存区的修改撤销掉（unstage），重新放回工作区</span><br><span class="line">git checkout -- &lt;file&gt;  %把在工作区的修改全部撤销</span><br></pre></td></tr></table></figure>

<h2 id="删除-amp-恢复"><a href="#删除-amp-恢复" class="headerlink" title="删除&amp;恢复"></a>删除&amp;恢复</h2><h4 id="删除文件"><a href="#删除文件" class="headerlink" title="删除文件"></a>删除文件</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git rm test.txt </span><br><span class="line">git commit -m &quot;remove test.txt&quot;</span><br></pre></td></tr></table></figure>

<h4 id="恢复误删文件"><a href="#恢复误删文件" class="headerlink" title="恢复误删文件"></a>恢复误删文件</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git checkout -- test.txt</span><br></pre></td></tr></table></figure>

<h1 id="远程仓库"><a href="#远程仓库" class="headerlink" title="远程仓库"></a>远程仓库</h1><h4 id="关联仓库"><a href="#关联仓库" class="headerlink" title="关联仓库"></a>关联仓库</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git remote add origin </span><br><span class="line">git@github.com:&lt;github_user_name&gt;&#x2F;&lt;github_repo_name &gt;.git</span><br></pre></td></tr></table></figure>

<h4 id="推送到远程"><a href="#推送到远程" class="headerlink" title="推送到远程"></a>推送到远程</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push -u origin master  % 第一次</span><br><span class="line">$ git push origin master</span><br></pre></td></tr></table></figure>

<h4 id="克隆"><a href="#克隆" class="headerlink" title="克隆"></a>克隆</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone git@github.com:m&lt;user_name&gt;&#x2F;&lt;repo_name&gt;.git</span><br><span class="line">git clone https:&#x2F;&#x2F;github.com&#x2F;&lt;user_name&gt;&#x2F;&lt;repo_name&gt;.git</span><br></pre></td></tr></table></figure>

<h1 id="分支管理"><a href="#分支管理" class="headerlink" title="分支管理"></a>分支管理</h1><h4 id="创建切换"><a href="#创建切换" class="headerlink" title="创建切换"></a>创建切换</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git checkout -b dev 创建并切换</span><br><span class="line">$ git branch dev     % 创建</span><br><span class="line">$ git checkout dev   % 切换</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git switch -c dev   %创建并切换</span><br><span class="line">$ git switch master   %创建</span><br></pre></td></tr></table></figure>

<h4 id="查看分支"><a href="#查看分支" class="headerlink" title="查看分支"></a>查看分支</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch</span><br></pre></td></tr></table></figure>

<h4 id="合并"><a href="#合并" class="headerlink" title="合并"></a>合并</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git merge dev     %用于合并指定分支到当前分支</span><br><span class="line">$ git merge --no-ff -m &quot;&lt;message&gt;&quot; dev   %合并后显示分支信息</span><br></pre></td></tr></table></figure>

<h4 id="删除分支"><a href="#删除分支" class="headerlink" title="删除分支"></a>删除分支</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch -d dev</span><br><span class="line">git branch -D &lt;name   % 强行删除未合并分支</span><br></pre></td></tr></table></figure>

<h4 id="储藏现场"><a href="#储藏现场" class="headerlink" title="储藏现场"></a>储藏现场</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git stash</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git stash list    %查看工作现场</span><br></pre></td></tr></table></figure>

<h4 id="恢复现场"><a href="#恢复现场" class="headerlink" title="恢复现场"></a>恢复现场</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git stash apply</span><br><span class="line">git stash apply stash@&#123;0&#125;</span><br><span class="line"></span><br><span class="line">git stash drop</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git stash pop</span><br></pre></td></tr></table></figure>

<h4 id="复制一个特定的提交到当前分支"><a href="#复制一个特定的提交到当前分支" class="headerlink" title="复制一个特定的提交到当前分支"></a>复制一个特定的提交到当前分支</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git cherry-pick &lt;commit&gt;</span><br></pre></td></tr></table></figure>

<h4 id="查看远程库的信息"><a href="#查看远程库的信息" class="headerlink" title="查看远程库的信息"></a>查看远程库的信息</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git remote</span><br><span class="line">$ git remote -v   %详细信息</span><br></pre></td></tr></table></figure>

<h3 id="推动分支"><a href="#推动分支" class="headerlink" title="推动分支"></a>推动分支</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push origin master</span><br><span class="line">$ git push origin dev</span><br></pre></td></tr></table></figure>

<h4 id="创建远程origin的dev分支到本地"><a href="#创建远程origin的dev分支到本地" class="headerlink" title="创建远程origin的dev分支到本地"></a>创建远程<code>origin</code>的<code>dev</code>分支到本地</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git checkout -b dev origin&#x2F;dev</span><br></pre></td></tr></table></figure>

<h4 id="指定本地dev分支与远程origin-dev分支的链接"><a href="#指定本地dev分支与远程origin-dev分支的链接" class="headerlink" title="指定本地dev分支与远程origin/dev分支的链接"></a>指定本地<code>dev</code>分支与远程<code>origin/dev</code>分支的链接</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch --set-upstream-to&#x3D;origin&#x2F;dev dev</span><br></pre></td></tr></table></figure>

<h1 id="标签管理"><a href="#标签管理" class="headerlink" title="标签管理"></a>标签管理</h1><h4 id="打标签"><a href="#打标签" class="headerlink" title="打标签"></a>打标签</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git tag v1.0   % 打在最新提交的commit上</span><br><span class="line">$ git tag v0.9 f52c633  %打在指定commit上</span><br><span class="line">$ git tag -a v0.1 -m &quot;version 0.1 released&quot; 1094adb   %指定标签名&amp;说明文字</span><br></pre></td></tr></table></figure>

<h4 id="查看所有标签"><a href="#查看所有标签" class="headerlink" title="查看所有标签"></a>查看所有标签</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git tag</span><br></pre></td></tr></table></figure>

<h4 id="查看标签信息"><a href="#查看标签信息" class="headerlink" title="查看标签信息"></a>查看标签信息</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git show v0.9</span><br></pre></td></tr></table></figure>

<h4 id="本地删除"><a href="#本地删除" class="headerlink" title="本地删除"></a>本地删除</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git tag -d v0.1</span><br></pre></td></tr></table></figure>

<h4 id="推送到远程-1"><a href="#推送到远程-1" class="headerlink" title="推送到远程"></a>推送到远程</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push origin v1.0</span><br><span class="line">$ git push origin --tags    % 推送所有</span><br></pre></td></tr></table></figure>

<h4 id="删除远程标签"><a href="#删除远程标签" class="headerlink" title="删除远程标签"></a>删除远程标签</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git tag -d v0.9		% 本地删除</span><br><span class="line">$ git push origin :refs&#x2F;tags&#x2F;v0.9	% 远程删除</span><br></pre></td></tr></table></figure>

<h1 id="自定义"><a href="#自定义" class="headerlink" title="自定义"></a>自定义</h1><h4 id="显示颜色"><a href="#显示颜色" class="headerlink" title="显示颜色"></a>显示颜色</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git config --global color.ui true</span><br></pre></td></tr></table></figure>

<h4 id="忽略特殊文件"><a href="#忽略特殊文件" class="headerlink" title="忽略特殊文件"></a>忽略特殊文件</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git add -f App.class   %添加已经被忽略的文件</span><br><span class="line">$ git check-ignore -v App.class  %检查ignore</span><br></pre></td></tr></table></figure>

<h4 id="配置别名"><a href="#配置别名" class="headerlink" title="配置别名"></a>配置别名</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git config --global alias.st status   %有空格要&#39;&#39;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>git</category>
      </categories>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>git</title>
    <url>/2020/06/17/git-tutotial/</url>
    <content><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><h2 id="什么是git"><a href="#什么是git" class="headerlink" title="什么是git"></a>什么是git</h2><p>分布式版本控制系统</p>
<a id="more"></a>

<h2 id="集中式-vs-分布式-版本控制系统"><a href="#集中式-vs-分布式-版本控制系统" class="headerlink" title="集中式 vs 分布式 版本控制系统"></a>集中式 vs 分布式 版本控制系统</h2><p>集中式版本控制系统版本库存放在中央处理器，需要联网，服务器挂了就都挂了</p>
<p>分布式版本控制系统，没有“中央服务器”，每台电脑都是完整的版本库，不需要联网，安全性高</p>
<h1 id="创建"><a href="#创建" class="headerlink" title="创建"></a>创建</h1><h2 id="创建版本库（repository）"><a href="#创建版本库（repository）" class="headerlink" title="创建版本库（repository）"></a>创建版本库（repository）</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkdir &lt;name for file&gt;</span><br><span class="line">cd &lt;file path&gt;</span><br><span class="line">git init</span><br></pre></td></tr></table></figure>

<p>过<code>git init</code>命令把这个目录变成Git可以管理的仓库，生成<code>.git</code>目录（隐藏）</p>
<h2 id="添加文件到版本库—注意事项"><a href="#添加文件到版本库—注意事项" class="headerlink" title="添加文件到版本库—注意事项"></a>添加文件到版本库—注意事项</h2><p>版本控制系统，本质是跟踪文本文件的改动。所以图片片、视频这些二进制文件，虽然也能由版本控制系统管理，但没法跟踪文件的变化，只能把二进制文件每次改动串起来，也就是只知道图片从100KB改成了120KB，但到底改了啥，版本控制系统不知道，也没法知道。</p>
<p>Microsoft的Word格式是二进制格式，所以要用纯文本编写文件</p>
<p>万不要使用Windows自带的<strong>记事本</strong>编辑任何文本文件。原因是Microsoft开发记事本的团队使用了一个非常弱智的行为来保存UTF-8编码的文件，他们自作聪明地在每个文件开头添加了0xefbbbf（十六进制）的字符，会带来很多错误。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git add &lt;file&gt;</span><br><span class="line">git commit -m &lt;message&gt;</span><br></pre></td></tr></table></figure>

<h1 id="版本回退、修改、删除"><a href="#版本回退、修改、删除" class="headerlink" title="版本回退、修改、删除"></a>版本回退、修改、删除</h1><h2 id="查看"><a href="#查看" class="headerlink" title="查看"></a>查看</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git status %查看仓库状态,是否有文件被修改过</span><br></pre></td></tr></table></figure>

<p>工作区（<strong>working tree</strong>），暂存区（<strong>index /stage</strong>），本地仓库（<strong>repository</strong>）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git diff &lt;file&gt;  %working tree与index差别</span><br><span class="line">git diff --cached &lt;file&gt;  %index与repository差别</span><br><span class="line">git diff HEAD &lt;file&gt;  %查看working tree和repository差别。HEAD代表的是新commit的信息。</span><br></pre></td></tr></table></figure>

<p><img src="https://www.liaoxuefeng.com/files/attachments/919020037470528/0" alt="git-repo"></p>
<h2 id="版本回退至最近的commit"><a href="#版本回退至最近的commit" class="headerlink" title="版本回退至最近的commit"></a>版本回退至最近的commit</h2><h3 id="查看版本号"><a href="#查看版本号" class="headerlink" title="查看版本号"></a>查看版本号</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git log %查看提交日志</span><br></pre></td></tr></table></figure>

<p>输出信息（eg）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git log</span><br><span class="line"></span><br><span class="line">commit 1094adb7b9b3807259d8cb349e7df1d4d6477073 (HEAD -&gt; master)	% 版本号</span><br><span class="line">Author: Michael Liao &lt;askxuefeng@gmail.com&gt;</span><br><span class="line">Date:   Fri May 18 21:06:15 2018 +0800</span><br><span class="line"></span><br><span class="line">    append GPL	% &lt;message&gt;</span><br><span class="line"></span><br><span class="line">commit e475afc93c209a690c39c13a46716e8fa000c366</span><br><span class="line">Author: Michael Liao &lt;askxuefeng@gmail.com&gt;</span><br><span class="line">Date:   Fri May 18 21:03:36 2018 +0800</span><br><span class="line"></span><br><span class="line">    add distributed</span><br><span class="line"></span><br><span class="line">commit eaadf4e385e865d25c48e7ca9c8395c3f7dfaef0</span><br><span class="line">Author: Michael Liao &lt;askxuefeng@gmail.com&gt;</span><br><span class="line">Date:   Fri May 18 20:59:18 2018 +0800</span><br><span class="line"></span><br><span class="line">    wrote a readme file</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git log --pretty&#x3D;oneline</span><br></pre></td></tr></table></figure>

<p>输出信息（eg）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git log --pretty&#x3D;oneline</span><br><span class="line"></span><br><span class="line">1094adb7b9b3807259d8cb349e7df1d4d6477073 (HEAD -&gt; master) append GPL</span><br><span class="line">e475afc93c209a690c39c13a46716e8fa000c366 add distributed</span><br><span class="line">eaadf4e385e865d25c48e7ca9c8395c3f7dfaef0 wrote a readme file</span><br></pre></td></tr></table></figure>

<ul>
<li>版本号（commit id）是一个很大的十六进制数。多人在同一个版本库里工作，如果大家都用1，2，3……作为版本号，那肯定就冲突了</li>
<li><code>HEAD</code> 表示当前版本，<code>HEAD^</code>上一个版本,<code>HEAD^^</code>上上版本，<code>HEAD~100</code>上一百个版本</li>
</ul>
<h3 id="回退"><a href="#回退" class="headerlink" title="回退"></a>回退</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reset --hard HEAD^       %回退到上一个版本</span><br></pre></td></tr></table></figure>

<p>若想再回到最新版本，需要找到它的版本号。此时使用<code>git log</code>看不到最新版本，使用<code>git reflog</code>查看每一次命令。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reset --hard 1094a      %版本号没必要写全，前几位就可以了，Git会自动去找</span><br></pre></td></tr></table></figure>

<h3 id="撤销修改"><a href="#撤销修改" class="headerlink" title="撤销修改"></a>撤销修改</h3><h4 id="撤销工作区"><a href="#撤销工作区" class="headerlink" title="撤销工作区"></a>撤销工作区</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git checkout -- &lt;file&gt;  %把在工作区的修改全部撤销</span><br></pre></td></tr></table></figure>

<p>即回到最近一次<code>git commit</code>或<code>git add</code>时的状态</p>
<p><strong>不要忘记<code>--</code>  ， <code>git checkout &lt;&gt;</code>是切换到另一个分支</strong></p>
<h4 id="撤销暂存区"><a href="#撤销暂存区" class="headerlink" title="撤销暂存区"></a>撤销暂存区</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reset HEAD &lt;file&gt; %把暂存区的修改撤销掉（unstage），重新放回工作区</span><br><span class="line">git checkout -- &lt;file&gt;  %把在工作区的修改全部撤销</span><br></pre></td></tr></table></figure>

<h4 id="撤销版本库"><a href="#撤销版本库" class="headerlink" title="撤销版本库"></a>撤销版本库</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reset --hard HEAD^       %回退到上一个版本</span><br></pre></td></tr></table></figure>

<p>前提：未推送到远程仓库</p>
<h3 id="删除文件"><a href="#删除文件" class="headerlink" title="删除文件"></a>删除文件</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git rm test.txt</span><br><span class="line">git commit -m &quot;remove test.txt&quot;</span><br></pre></td></tr></table></figure>

<h3 id="恢复误删文件"><a href="#恢复误删文件" class="headerlink" title="恢复误删文件"></a>恢复误删文件</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git checkout -- test.txt</span><br></pre></td></tr></table></figure>

<p>注意：从来没有被添加到版本库就被删除的文件，是无法恢复的</p>
<h1 id="远程仓库"><a href="#远程仓库" class="headerlink" title="远程仓库"></a>远程仓库</h1><h2 id="基本"><a href="#基本" class="headerlink" title="基本"></a>基本</h2><ul>
<li><p>本地Git仓库和GitHub仓库之间的传输是通过SSH加密</p>
</li>
<li><p>用户主目录 -&gt; .ssh目录     找到对应的<code>id_rsa</code>和<code>id_rsa.pub</code>两个文件</p>
</li>
<li><p><code>id_rsa</code>是私钥，不能泄露出去;<code>id_rsa.pub</code>是公钥，可以告诉任何人</p>
</li>
<li><p>登陆GitHub，点“Add SSH Key”，填上任意Title，在Key文本框里粘贴<code>id_rsa.pub</code>文件的内容</p>
</li>
</ul>
<h2 id="本地库添加到远程库（不常用，一般克隆）"><a href="#本地库添加到远程库（不常用，一般克隆）" class="headerlink" title="本地库添加到远程库（不常用，一般克隆）"></a>本地库添加到远程库（不常用，一般克隆）</h2><ol>
<li><p>创建本地仓库</p>
</li>
<li><p>github上创建仓库</p>
</li>
<li><p>关联仓库</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git remote add origin </span><br><span class="line">git@github.com:&lt;github_user_name&gt;&#x2F;&lt;github_repo_name &gt;.git</span><br></pre></td></tr></table></figure>

<p>这里是ssh推送，速度快；也可以使用http推送,但是每次需要输入口令。</p>
</li>
<li><p>将本地库master分支推送到远程</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push -u origin master</span><br></pre></td></tr></table></figure>

<p>远程库是空的，我们第一次推送<code>master</code>分支时，加上了<code>-u</code>参数，Git不但会把本地的<code>master</code>分支内容推送的远程新的<code>master</code>分支，还会把本地的<code>master</code>分支和远程的<code>master</code>分支关联起来，在以后的推送或者拉取时就可以简化命令为</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push origin master</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="从远程库克隆"><a href="#从远程库克隆" class="headerlink" title="从远程库克隆"></a>从远程库克隆</h2><ol>
<li><p>github上创建仓库</p>
</li>
<li><p>克隆到本地</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone git@github.com:m&lt;user_name&gt;&#x2F;&lt;repo_name&gt;.git</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https:&#x2F;&#x2F;github.com&#x2F;&lt;user_name&gt;&#x2F;&lt;repo_name&gt;.git</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h1 id="分支管理"><a href="#分支管理" class="headerlink" title="分支管理"></a>分支管理</h1><h2 id="创建与合并分支"><a href="#创建与合并分支" class="headerlink" title="创建与合并分支"></a>创建与合并分支</h2><h3 id="理解"><a href="#理解" class="headerlink" title="理解"></a>理解</h3><ol>
<li>一开始的时候，<code>master</code>分支是一条线，Git用<code>master</code>指向最新的提交，再用<code>HEAD</code>指向<code>master</code>，就能确定当前分支，以及当前分支的提交点</li>
</ol>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919022325462368/0" alt="git-br-initial"></p>
<ol start="2">
<li>当我们创建新的分支，例如<code>dev</code>时，Git新建了一个指针叫<code>dev</code>，指向<code>master</code>相同的提交，再把<code>HEAD</code>指向<code>dev</code>，就表示当前分支在<code>dev</code>上</li>
</ol>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919022363210080/l" alt="git-br-create"></p>
<ol start="3">
<li>从现在开始，对工作区的修改和提交就是针对<code>dev</code>分支了，比如新提交一次后，<code>dev</code>指针往前移动一步，而<code>master</code>指针不变</li>
</ol>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919022387118368/l" alt="git-br-dev-fd"></p>
<ol start="4">
<li>假如我们在<code>dev</code>上的工作完成了，就可以把<code>dev</code>合并到<code>master</code>上。Git怎么合并呢？最简单的方法，就是直接把<code>master</code>指向<code>dev</code>的当前提交，就完成了合并</li>
</ol>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919022412005504/0" alt="git-br-ff-merge"></p>
<ol start="5">
<li>合并完分支后，甚至可以删除<code>dev</code>分支。删除<code>dev</code>分支就是把<code>dev</code>指针给删掉，删掉后，我们就剩下了一条<code>master</code>分支</li>
</ol>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919022479428512/0" alt="git-br-rm"></p>
<h3 id="实战"><a href="#实战" class="headerlink" title="实战"></a>实战</h3><ol>
<li><p>创建<code>dev</code>分支，然后切换到<code>dev</code>分支</p>
<p>1.1 法一</p>
   <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git checkout -b dev</span><br></pre></td></tr></table></figure>

<p><code>it checkout</code>命令加上<code>-b</code>参数表示创建并切换，相当于以下两条命令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch dev     % 创建</span><br><span class="line">$ git checkout dev   % 切换</span><br></pre></td></tr></table></figure>

<ul>
<li>撤销修改则是<code>git checkout -- &lt;file&gt;</code></li>
</ul>
<p>1.2 法二</p>
<p>切换分支这个动作，用<code>switch</code>更科学</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git switch -c dev   %创建并切换</span><br><span class="line">$ git switch master   %创建</span><br></pre></td></tr></table></figure>

<hr>
<p>用<code>git branch</code>命令查看当前分支</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch</span><br><span class="line">* dev   % 当前分支前面会标一个*号</span><br><span class="line">  master</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>dev</code>分支上修改，提交</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git add readme.txt </span><br><span class="line">$ git commit -m &quot;branch test&quot;</span><br></pre></td></tr></table></figure>
</li>
<li><p>切换回master</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git checkout master</span><br></pre></td></tr></table></figure>

<p><img src="https://www.liaoxuefeng.com/files/attachments/919022533080576/0" alt="git-br-on-master"></p>
</li>
<li><p>把<code>dev</code>分支的工作成果合并到<code>master</code>分支</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git merge dev     %用于合并指定分支到当前分支</span><br><span class="line"></span><br><span class="line">Updating d46f35e..b17d20e</span><br><span class="line">Fast-forward</span><br><span class="line"> readme.txt | 1 +</span><br><span class="line"> 1 file changed, 1 insertion(+)</span><br></pre></td></tr></table></figure>

<p><code>Fast-forward</code>告诉我们这次合并是“快进模式”，也就是直接把<code>master</code>指向<code>dev</code>的当前提交，所以合并速度非常快。</p>
</li>
<li><p>删除<code>dev</code>分支</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch -d dev</span><br></pre></td></tr></table></figure>


</li>
</ol>
<h2 id="解决冲突"><a href="#解决冲突" class="headerlink" title="解决冲突"></a>解决冲突</h2><p>原因：<code>master</code>分支和<code>feature1</code>分支各自都分别有新的提交</p>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919023000423040/0" alt="git-br-feature1"></p>
<p>这种情况下，Git无法执行“快速合并”，必须手动解决冲突后再提交</p>
<ol>
<li><p>找到冲突文件（2种）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git merge feature1</span><br><span class="line">Auto-merging readme.txt</span><br><span class="line">CONFLICT (content): Merge conflict in readme.txt</span><br><span class="line">Automatic merge failed; fix conflicts and then commit the result.</span><br></pre></td></tr></table></figure>

<p><code>git status</code>也可以告诉我们冲突的文件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git status</span><br></pre></td></tr></table></figure>
</li>
<li><p>修改冲突文件。在冲突文件中Git用<code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>，<code>=======</code>，<code>&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code>标记出不同分支的内容，例如</p>
<blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt;Git is a distributed version control system.</span><br><span class="line">&gt;Git is free software distributed under the GPL.</span><br><span class="line">&gt;Git has a mutable index called stage.</span><br><span class="line">&gt;Git tracks changes of files.</span><br><span class="line">&gt;&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</span><br><span class="line">&gt;Creating a new branch is quick &amp; simple.</span><br><span class="line">&gt;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;</span><br><span class="line">&gt;Creating a new branch is quick AND simple.</span><br><span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature1</span><br></pre></td></tr></table></figure>
</blockquote>
</li>
<li><p>提交。<code>master</code>分支和<code>feature1</code>分支变成了下图所示</p>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919023031831104/0" alt="git-br-conflict-merged"></p>
<p>用带参数的<code>git log</code>也可以看到分支的合并情况</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git log --graph </span><br><span class="line">$ git log --graph --pretty&#x3D;oneline --abbrev-commit % (好看的)</span><br></pre></td></tr></table></figure>
</li>
<li><p>删除分支</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch -d f&lt;name&gt;</span><br></pre></td></tr></table></figure>


</li>
</ol>
<h3 id="分支管理策略"><a href="#分支管理策略" class="headerlink" title="分支管理策略"></a>分支管理策略</h3><h4 id="合并后显示分支信息"><a href="#合并后显示分支信息" class="headerlink" title="合并后显示分支信息"></a>合并后显示分支信息</h4><p>合并分支时，如果可能，Git会用<code>Fast forward</code>模式，但这种模式下，删除分支后，会丢掉分支信息。</p>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919022412005504/0" alt="git-br-ff-merge"></p>
<p>要强制禁用<code>Fast forward</code>模式，Git就会在merge时生成一个新的commit，这样，从分支历史上就可以看出分支信息</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git merge --no-ff -m &quot;merge with no-ff&quot; dev</span><br></pre></td></tr></table></figure>

<ul>
<li><code>--no-ff</code>参数，表示禁用<code>Fast forward</code>：</li>
<li>本次合并要创建一个新的commit，所以加上<code>-m</code>参数，把commit描述写进去</li>
</ul>
<p>合并后，我们用<code>git log</code>看看分支历史</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git log --graph --pretty&#x3D;oneline --abbrev-commit</span><br><span class="line">*   e1e9c68 (HEAD -&gt; master) merge with no-ff</span><br><span class="line">|\  </span><br><span class="line">| * f52c633 (dev) add merge</span><br><span class="line">|&#x2F;  </span><br><span class="line">*   cf810e4 conflict fixed</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p><img src="https://www.liaoxuefeng.com/files/attachments/919023225142304/0" alt="git-no-ff-mode"></p>
<h4 id="常见分支策略"><a href="#常见分支策略" class="headerlink" title="常见分支策略"></a>常见分支策略</h4><ul>
<li><code>master</code>分支应该是非常稳定的，也就是仅用来发布新版本，平时不能在上面干活</li>
<li>干活都在<code>dev</code>分支，版本发布时，再把<code>dev</code>分支合并到<code>master</code>上，再在<code>master</code>发布</li>
<li>每个人都在<code>dev</code>分支上干活，每个人都有自己的分支，时不时地往<code>dev</code>分支上合并</li>
</ul>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919023260793600/0" alt="git-br-policy"></p>
<h2 id="bug分支"><a href="#bug分支" class="headerlink" title="bug分支"></a>bug分支</h2><p>每个bug都可以通过一个新的临时分支来修复，修复后，合并分支，然后将临时分支删除。当你接到一个修复一个代号101的bug的任务时，很自然地，你想创建一个分支<code>issue-101</code>来修复它，但是，当前正在<code>dev</code>上进行的工作还没有提交，工作只进行到一半，还没法提交。</p>
<p>Git还提供了一个<code>stash</code>功能，可以把当前工作现场“储藏”起来，等以后恢复现场后继续工作：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git stash</span><br></pre></td></tr></table></figure>

<p>用<code>git status</code>查看工作区，就是干净的。</p>
<p>首先确定要在哪个分支上修复bug，假定需要在<code>master</code>分支上修复，就从<code>master</code>创建临时分支。修复bug，然后提交</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git commit -m &quot;fix bug 101&quot;</span><br><span class="line">[issue-101 4c805e2] fix bug 101</span><br><span class="line"> 1 file changed, 1 insertion(+), 1 deletion(-)</span><br></pre></td></tr></table></figure>

<p>修复完成后，切换到<code>master</code>分支，并完成合并，最后删除<code>issue-101</code>分支</p>
<p>接着回到<code>dev</code>分支干活</p>
<p>工作区是干净的，刚才的工作现场存到哪去了？用<code>git stash list</code>命令看看</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git stash list</span><br><span class="line">stash@&#123;0&#125;: WIP on dev: f52c633 add merge</span><br></pre></td></tr></table></figure>

<p>工作现场还在，Git把stash内容存在某个地方了，但是需要恢复一下，有两个办法：</p>
<ul>
<li><p>用<code>git stash apply</code>恢复，但是恢复后，stash内容并不删除，你需要用<code>git stash drop</code>来删除；</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git stash apply</span><br><span class="line">git stash drop</span><br></pre></td></tr></table></figure>
</li>
<li><p>另一种方式是用<code>git stash pop</code>，恢复的同时把stash内容也删了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git stash pop</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>再用<code>git stash list</code>查看，就看不到任何stash内容了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git stash list</span><br></pre></td></tr></table></figure>

<p>你可以多次stash，恢复的时候，先用<code>git stash list</code>查看，然后恢复指定的stash，用命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git stash apply stash@&#123;0&#125;</span><br></pre></td></tr></table></figure>

<p>在master分支上修复了bug后，我们要想一想，dev分支是早期从master分支分出来的，所以，这个bug其实在当前dev分支上也存在。同样的bug，要在dev上修复，我们只需要把<code>4c805e2 fix bug 101</code>这个提交所做的修改“复制”到dev分支。</p>
<p>为了方便操作，Git专门提供了一个<code>cherry-pick</code>命令<code>git cherry-pick &lt;commit&gt;</code>，让我们能复制一个特定的提交到当前分支</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch</span><br><span class="line">* dev</span><br><span class="line">  master</span><br><span class="line">$ git cherry-pick 4c805e2</span><br><span class="line">[master 1d4b803] fix bug 101</span><br><span class="line"> 1 file changed, 1 insertion(+), 1 deletion(-)</span><br></pre></td></tr></table></figure>

<p>既然可以在master分支上修复bug后，在dev分支上可以“重放”这个修复过程，那么直接在dev分支上修复bug，然后在master分支上“重放”行不行？当然可以，不过你仍然需要<code>git stash</code>命令保存现场，才能从dev分支切换到master分支。</p>
<h2 id="feature分支删除"><a href="#feature分支删除" class="headerlink" title="feature分支删除"></a>feature分支删除</h2><p>开发一个新feature，最好新建一个分支；</p>
<p>如果要丢弃一个没有被合并过的分支，可以通过<code>git branch -D &lt;name&gt;</code>强行删除。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch -d feature-vulcan</span><br><span class="line">error: The branch &#39;feature-vulcan&#39; is not fully merged.</span><br><span class="line">If you are sure you want to delete it, run &#39;git branch -D feature-vulcan&#39;.</span><br></pre></td></tr></table></figure>

<h2 id="多人协作"><a href="#多人协作" class="headerlink" title="多人协作"></a>多人协作</h2><p>当你从远程仓库克隆时，实际上Git自动把本地的<code>master</code>分支和远程的<code>master</code>分支对应起来了，并且，远程仓库的默认名称是<code>origin</code>。</p>
<p>要查看远程库的信息，用<code>git remote</code>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git remote</span><br><span class="line">origin</span><br></pre></td></tr></table></figure>

<p>或者，用<code>git remote -v</code>显示更详细的信息：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git remote -v</span><br><span class="line">origin  git@github.com:michaelliao&#x2F;learngit.git (fetch)</span><br><span class="line">origin  git@github.com:michaelliao&#x2F;learngit.git (push)</span><br></pre></td></tr></table></figure>

<p>上面显示了可以抓取和推送的<code>origin</code>的地址。如果没有推送权限，就看不到push的地址。</p>
<h3 id="推动分支："><a href="#推动分支：" class="headerlink" title="推动分支："></a>推动分支：</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push origin master</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push origin dev</span><br></pre></td></tr></table></figure>

<ul>
<li><p><code>master</code>分支是主分支，因此要时刻与远程同步；</p>
</li>
<li><p><code>dev</code>分支是开发分支，团队所有成员都需要在上面工作，所以也需要与远程同步；</p>
</li>
<li><p>bug分支只用于在本地修复bug，就没必要推到远程了，除非老板要看看你每周到底修复了几个bug；</p>
</li>
<li><p>feature分支是否推到远程，取决于你是否和你的小伙伴合作在上面开发</p>
</li>
</ul>
<h3 id="抓取分支"><a href="#抓取分支" class="headerlink" title="抓取分支"></a>抓取分支</h3><p>多人协作时，大家都会往<code>master</code>和<code>dev</code>分支上推送各自的修改。</p>
<p>当你的小伙伴从远程库clone时，默认情况下，你的小伙伴只能看到本地的<code>master</code>分支。不信可以用<code>git branch</code>命令看看：</p>
<p>现在，你的小伙伴要在<code>dev</code>分支上开发，就必须创建远程<code>origin</code>的<code>dev</code>分支到本地，于是他用这个命令创建本地<code>dev</code>分支</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git checkout -b dev origin&#x2F;dev</span><br></pre></td></tr></table></figure>

<p>现在，他就可以在<code>dev</code>上继续修改，然后，时不时地把<code>dev</code>分支<code>push</code>到远程：</p>
<p>你的小伙伴已经向<code>origin/dev</code>分支推送了他的提交，而碰巧你也对同样的文件作了修改，并试图推送。</p>
<p>推送失败，因为你的小伙伴的最新提交和你试图推送的提交有冲突，解决办法也很简单，Git已经提示我们，先用<code>git pull</code>把最新的提交从<code>origin/dev</code>抓下来，然后，在本地合并，解决冲突，再推送：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push origin dev</span><br><span class="line"></span><br><span class="line">error: failed to push some refs </span><br><span class="line">hint: Updates were rejected because the tip of your current branch is behind</span><br><span class="line">hint: its remote counterpart. Integrate the remote changes (e.g.</span><br><span class="line">hint: &#39;git pull ...&#39;) before pushing again.</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git pull</span><br><span class="line">There is no tracking information for the current branch.</span><br><span class="line">Please specify which branch you want to merge with.</span><br><span class="line">See git-pull(1) for details.</span><br><span class="line"></span><br><span class="line">    git pull &lt;remote&gt; &lt;branch&gt;</span><br><span class="line"></span><br><span class="line">If you wish to set tracking information for this branch you can do so with:</span><br><span class="line"></span><br><span class="line">    git branch --set-upstream-to&#x3D;origin&#x2F;&lt;branch&gt; dev</span><br></pre></td></tr></table></figure>

<p><code>git pull</code>也失败了，原因是没有指定本地<code>dev</code>分支与远程<code>origin/dev</code>分支的链接，根据提示，设置<code>dev</code>和<code>origin/dev</code>的链接</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git branch --set-upstream-to&#x3D;origin&#x2F;dev dev</span><br><span class="line">Branch &#39;dev&#39; set up to track remote branch &#39;dev&#39; from &#39;origin&#39;.</span><br></pre></td></tr></table></figure>

<p>这回<code>git pull</code>成功，但是合并有冲突，需要手动解决，解决的方法和分支管理中的解决冲突完全一样。解决后，提交，再push。</p>
<hr>
<p>因此，多人协作的工作模式通常是这样：</p>
<ol>
<li>首先，可以试图用<code>git push origin &lt;branch-name&gt;</code>推送自己的修改；</li>
<li>如果推送失败，则因为远程分支比你的本地更新，需要先用<code>git pull</code>试图合并；</li>
<li>如果合并有冲突，则解决冲突，并在本地提交；</li>
<li>没有冲突或者解决掉冲突后，再用<code>git push origin &lt;branch-name&gt;</code>推送就能成功！</li>
</ol>
<p>如果<code>git pull</code>提示<code>no tracking information</code>，则说明本地分支和远程分支的链接关系没有创建，用命令<code>git branch --set-upstream-to &lt;branch-name&gt; origin/&lt;branch-name&gt;</code>。</p>
<p>这就是多人协作的工作模式，一旦熟悉了，就非常简单。</p>
<h1 id="标签管理"><a href="#标签管理" class="headerlink" title="标签管理"></a>标签管理</h1><p>发布一个版本时，我们通常先在版本库中打一个标签（tag）。</p>
<p>tag是指向某个commit的指针（跟分支很像对不对？但是分支可以移动，标签不能移动），所以，创建和删除标签都是瞬间完成的。</p>
<blockquote>
<p>“请把上周一的那个版本打包发布，commit号是6a5819e…”</p>
<p>“请把上周一的那个版本打包发布，版本号是v1.2”</p>
</blockquote>
<p>tag就是一个让人容易记住的有意义的名字，它跟某个commit绑在一起</p>
<h2 id="创建标签"><a href="#创建标签" class="headerlink" title="创建标签"></a>创建标签</h2><p><code>git tag &lt;name&gt;</code>就可以打一个新标签：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git tag v1.0   % 打在最新提交的commit上</span><br><span class="line">$ git tag v0.9 f52c633  %打在指定commit上</span><br></pre></td></tr></table></figure>

<p>创建带有说明的标签，用<code>-a</code>指定标签名，<code>-m</code>指定说明文字</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git tag -a v0.1 -m &quot;version 0.1 released&quot; 1094adb</span><br></pre></td></tr></table></figure>

<p>可以用命令<code>git tag</code>查看所有标签（标签不是按时间顺序列出，而是按字母排序的）：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git tag</span><br></pre></td></tr></table></figure>

<p>用<code>git show &lt;tagname&gt;</code>查看标签信息：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git show v0.9</span><br></pre></td></tr></table></figure>

<h2 id="操作标签"><a href="#操作标签" class="headerlink" title="操作标签"></a>操作标签</h2><p>创建的标签都只存储在本地，不会自动推送到远程。所以，打错的标签可以在本地安全删除</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git tag -d v0.1</span><br></pre></td></tr></table></figure>

<p>推送某个标签到远程，使用命令<code>git push origin &lt;tagname&gt;</code>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push origin v1.0</span><br></pre></td></tr></table></figure>

<p>一次性推送全部尚未推送到远程的本地标签：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push origin --tags</span><br></pre></td></tr></table></figure>

<p>删除远程标签就麻烦一点，先从本地删除<code>git tag -d &lt;tagname&gt;</code>，然后，从远程删除<code>git push origin :refs/tags/&lt;tagname&gt;</code></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git tag -d v0.9		% 本地删除</span><br><span class="line">$ git push origin :refs&#x2F;tags&#x2F;v0.9	% 远程删除</span><br></pre></td></tr></table></figure>

<h1 id="自定义git"><a href="#自定义git" class="headerlink" title="自定义git"></a>自定义git</h1><p>让Git显示颜色，会让命令输出看起来更醒目：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git config --global color.ui true</span><br></pre></td></tr></table></figure>

<h2 id="忽略特殊文件"><a href="#忽略特殊文件" class="headerlink" title="忽略特殊文件"></a>忽略特殊文件</h2><ul>
<li><p>有些时候，你必须把某些文件放到Git工作目录中，但又不能提交它们</p>
<p>Git工作区的根目录下创建一个特殊的<code>.gitignore</code>文件，然后把要忽略的文件名填进去</p>
</li>
<li><p>有些时候，你想添加一个文件到Git，但发现添加不了，原因是这个文件被<code>.gitignore</code>忽略了。如果你确实想添加该文件，可以用<code>-f</code>强制添加到Git：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git add -f App.class</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>.gitignore</code>写得有问题，需要找出来到底哪个规则写错了，可以用<code>git check-ignore</code>命令检查：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git check-ignore -v App.class</span><br><span class="line">.gitignore:3:*.class	App.class</span><br></pre></td></tr></table></figure>

<p>  Git会告诉我们，<code>.gitignore</code>的第3行规则忽略了该文件，于是我们就可以知道应该修订哪个规则。</p>
</li>
</ul>
<h2 id="配置别名"><a href="#配置别名" class="headerlink" title="配置别名"></a>配置别名</h2><p>如果敲<code>git st</code>就表示<code>git status</code>那就简单多了</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git config --global alias.st status</span><br></pre></td></tr></table></figure>



<p>甚至还有人丧心病狂地把<code>lg</code>配置成了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git config --global alias.lg &quot;log --color --graph --pretty&#x3D;format:&#39;%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)&lt;%an&gt;%Creset&#39; --abbrev-commit&quot;</span><br></pre></td></tr></table></figure>

<p>来看看<code>git lg</code>的效果：</p>
<p><img src="https://www.liaoxuefeng.com/files/attachments/919059728302912/0" alt="git-lg"></p>
<h2 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h2><p>加上<code>--global</code>是针对当前用户起作用的，如果不加，那只针对当前的仓库起作用</p>
<p>配置文件放哪了？</p>
<p>当前用户的Git配置文件放在用户主目录下的一个隐藏文件<code>.gitconfig</code>中</p>
<p>每个仓库的Git配置文件都放在<code>.git/config</code>文件中。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ cat .git&#x2F;config </span><br><span class="line">[core]</span><br><span class="line">    repositoryformatversion &#x3D; 0</span><br><span class="line">    filemode &#x3D; true</span><br><span class="line">    bare &#x3D; false</span><br><span class="line">    logallrefupdates &#x3D; true</span><br><span class="line">    ignorecase &#x3D; true</span><br><span class="line">    precomposeunicode &#x3D; true</span><br><span class="line">[remote &quot;origin&quot;]</span><br><span class="line">    url &#x3D; git@github.com:michaelliao&#x2F;learngit.git</span><br><span class="line">    fetch &#x3D; +refs&#x2F;heads&#x2F;*:refs&#x2F;remotes&#x2F;origin&#x2F;*</span><br><span class="line">[branch &quot;master&quot;]</span><br><span class="line">    remote &#x3D; origin</span><br><span class="line">    merge &#x3D; refs&#x2F;heads&#x2F;master</span><br><span class="line">[alias]</span><br><span class="line">    last &#x3D; log -1</span><br></pre></td></tr></table></figure>

<p>别名就在<code>[alias]</code>后面，要删除别名，直接把对应的行删掉即可。</p>
]]></content>
      <categories>
        <category>git</category>
      </categories>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>雅思口语-part1</title>
    <url>/2020/08/07/ielts-speaking1/</url>
    <content><![CDATA[<h2 id="your-study"><a href="#your-study" class="headerlink" title="your study"></a>your study</h2><a id="more"></a>

<p><code>volcabulary</code></p>
<ul>
<li>uestc: University of Electronic Science and Technology of China</li>
<li>college entrance examination</li>
<li>5G: fifth generation</li>
<li>south-west</li>
</ul>
<blockquote>
<p>study in the morning or night ? (night)</p>
<ul>
<li>typical night owl /aul/</li>
<li>challenging get up early / sleepy</li>
</ul>
</blockquote>
<blockquote>
<p>look forward to working </p>
<ul>
<li>earn money -&gt; buy things / rely less on parents</li>
<li>useful member of society -&gt; ahievemntment</li>
</ul>
</blockquote>
<blockquote>
<p>do u like ur subject</p>
<ul>
<li>practical -&gt; fifth generation</li>
<li>math physics -&gt; think logically  </li>
<li>expriments -&gt; problem-solving skills + achievement </li>
</ul>
</blockquote>
<blockquote>
<p>why choose to study at ur school</p>
<ul>
<li>has a good reputation -&gt; job market</li>
<li>a friendly-leanring environment -&gt; well equipped library </li>
<li>extracurricular activities -&gt;clubs + competition</li>
</ul>
</blockquote>
<blockquote>
<p>enjoy first day?</p>
<ul>
<li>well-equipment -&gt; sports center, swimming pool, library</li>
<li>meet my roommates -&gt; share same interests</li>
<li>four cafeterias -&gt; foodie</li>
</ul>
</blockquote>
<h2 id="your-hometown"><a href="#your-hometown" class="headerlink" title="your hometown"></a>your hometown</h2><ul>
<li>in the southeast of China </li>
<li>costs of living </li>
<li>housing </li>
<li>it’s an inland city and is surrounded by high moutains</li>
<li>the pace of life is slow</li>
<li>leisure facilities</li>
</ul>
<h1 id="film"><a href="#film" class="headerlink" title="film"></a>film</h1><ul>
<li>The Intouchables (触不可及)</li>
<li>The Intouchables has broken box office records in its native France and across Europe</li>
<li>comedy about friendship, trust, respect</li>
<li>Based on a true story of friendship between a handicap millionaire and a young man who is hired as his caregiver.</li>
<li>special effect </li>
<li>the film has a strong cast</li>
<li>comedies / romantic movie / sci-fi movie</li>
<li>lose my privary</li>
<li>sitimulate our imagination</li>
<li>sound quality</li>
</ul>
<h1 id="social-media"><a href="#social-media" class="headerlink" title="social media"></a>social media</h1><ul>
<li>personal data and privacy can be hacked</li>
</ul>
<h2 id="What-Are-The-Top-10-Reasons-For-Using-Social-Media"><a href="#What-Are-The-Top-10-Reasons-For-Using-Social-Media" class="headerlink" title="What Are The Top 10 Reasons For Using Social Media?"></a>What Are The Top 10 Reasons For Using Social Media?</h2><ol>
<li>To <strong>stay in touch</strong> with what friends are doing (42%)</li>
<li>To <strong>stay up-to-date</strong> with news and current events (41%)</li>
<li>To <strong>fill up spare time</strong> (39%)</li>
<li>To <strong>find funny or entertaining content</strong> (37%)</li>
</ol>
<h1 id="weather"><a href="#weather" class="headerlink" title="weather"></a>weather</h1><ul>
<li>“an early spring, a hot summer, a cool autumn and a relatively warm winter”.</li>
<li>This type of climate is mild and wet</li>
<li>Rain falls all year round with a peak in July and August</li>
<li>the days are longer and the nights are shorter</li>
<li>have heatstroke 中暑</li>
</ul>
<h1 id="music"><a href="#music" class="headerlink" title="music"></a>music</h1><ul>
<li>Energetic and rhythmic</li>
<li>be talented in sth  /  have a gift for sth</li>
</ul>
<h1 id="others"><a href="#others" class="headerlink" title="others"></a>others</h1><ul>
<li>diving 潜水（marine life / move underwater / like fly）</li>
<li>running (strengthen our hearts and lungs)</li>
<li>hairdresser 理发师</li>
<li>barbershop 理发店</li>
<li>durable 耐磨的</li>
<li>can be well matched with tight jean</li>
<li>husky</li>
<li>street cleaner</li>
<li>dust bin 垃圾桶</li>
<li>I have a bad memory   </li>
<li>on a random basis</li>
<li>lend something to someone</li>
<li>regulate</li>
<li>DMV ：department of motor vehicle</li>
<li>property company 房地产</li>
<li>deal with jet lag  倒时差</li>
<li>develope a film 洗照片</li>
</ul>
]]></content>
      <tags>
        <tag>雅思</tag>
      </tags>
  </entry>
  <entry>
    <title>雅思大作文</title>
    <url>/2020/07/16/ielts_writing1/</url>
    <content><![CDATA[<h1 id="day1-了解"><a href="#day1-了解" class="headerlink" title="day1 了解"></a>day1 了解</h1><a id="more"></a>

<ul>
<li><p>不能缩写（<code>can&#39;t</code> -&gt; <code>cannot</code>,  <code>won&#39;t</code>-&gt;<code>will not</code>）</p>
</li>
<li><p>不能口语化 <code>lots of</code>-&gt; <code>many/a large number of</code></p>
</li>
<li><p>不能祈使句。不用<code>！</code>等感情色彩强烈的符号结尾。不能太绝对。</p>
</li>
<li><p>不能谚语</p>
</li>
<li><p>适度使用<code>I/We</code></p>
</li>
</ul>
<h1 id="day2-充实观点streec"><a href="#day2-充实观点streec" class="headerlink" title="day2 充实观点streec"></a>day2 充实观点<code>streec</code></h1><table>
<thead>
<tr>
<th>skills</th>
<th align="center">efficiency</th>
</tr>
</thead>
<tbody><tr>
<td>technology</td>
<td align="center">safety（犯罪率，个人信息，病毒）</td>
</tr>
<tr>
<td>right(pension,paid vacations,freedom,privacy)</td>
<td align="center">responsibility（援助,学校社会媒体）</td>
</tr>
<tr>
<td>environment（污染，多样性）</td>
<td align="center">health（压力，医疗，运动饮食，心理）</td>
</tr>
<tr>
<td>employment（就业机会）</td>
<td align="center">money（贫富差距，纳税人负担，援助）</td>
</tr>
<tr>
<td>culture（传统，文化多样性）</td>
<td align="center">fun（探索发现，娱乐，动力）</td>
</tr>
</tbody></table>
<h1 id="day3-易错词"><a href="#day3-易错词" class="headerlink" title="day3 易错词"></a>day3 易错词</h1><ul>
<li><p>不可数</p>
<ul>
<li>feedback（反馈） </li>
<li>behaviour</li>
<li>knowledge </li>
<li>equipment</li>
<li>potential</li>
<li>furniture</li>
<li>scenery</li>
<li>jewllery</li>
</ul>
</li>
<li><p>outweigh（v.）结尾没有t</p>
</li>
<li><p>economy(经济) </p>
<p>economics（经济学） </p>
<p>economical（省钱的）</p>
</li>
<li><p>lack sth (v.) lack of sth (n.)</p>
</li>
<li><p>benefit (对…有益)  及物动词</p>
<p>benefit form （从…收益）</p>
</li>
</ul>
<h1 id="day4-作文结构"><a href="#day4-作文结构" class="headerlink" title="day4 作文结构"></a>day4 作文结构</h1><ul>
<li><p>偏向“折中式”，对正反两方进行辩证，但是更倾向一方</p>
</li>
<li><p>分类</p>
<ul>
<li>report (cause, solution, problems, factors, measures, effects)</li>
<li>argument (debate, pros/cons)</li>
</ul>
</li>
<li><p>四段式 / 五段式</p>
<table>
<thead>
<tr>
<th>四段式（两个主体段，一方一段）</th>
<th>五段式（三个主体段，两个一方，一个一方）</th>
</tr>
</thead>
<tbody><tr>
<td>1~3</td>
<td>1~3</td>
</tr>
<tr>
<td>1 + 6~8</td>
<td>1 + 2~4</td>
</tr>
<tr>
<td>1 + 4~6</td>
<td>1 + 2~4</td>
</tr>
<tr>
<td>1~2</td>
<td>1 ~ 2-4</td>
</tr>
<tr>
<td>\</td>
<td>1~2</td>
</tr>
<tr>
<td>每段2-3个分论点。适合分论点多，支持句少。</td>
<td>每段一个分论点。适合分论点少，支持句多。</td>
</tr>
</tbody></table>
</li>
</ul>
<h1 id="day4-开头段"><a href="#day4-开头段" class="headerlink" title="day4 开头段"></a>day4 开头段</h1><h2 id="agree-or-disagree"><a href="#agree-or-disagree" class="headerlink" title="agree  or disagree"></a>agree  or disagree</h2><ul>
<li>基础<ul>
<li>背景（optional）</li>
<li>改写观点（小手术）</li>
<li>自己观点<ul>
<li>明确支持/反对 <code>I agree/disagree with this view</code></li>
<li>主要支持/反对 <code>I tend to agree/disagree with this view</code></li>
<li>两方差不多 <code>I partly agree with this view</code></li>
</ul>
</li>
</ul>
</li>
<li>进阶<ul>
<li>背景</li>
<li>自己观点<ul>
<li><code>Although I agree with that... ,I believe that...</code></li>
<li><code>While I agree that..., I believe that ...</code></li>
</ul>
</li>
</ul>
</li>
</ul>
<p><strong>不能出现<code>others think that ...</code></strong></p>
<h2 id="双方讨论"><a href="#双方讨论" class="headerlink" title="双方讨论"></a>双方讨论</h2><p>一般是在主体段分别讨论两种看法，在结尾段再提出自己的看法<code>in my opinion</code> <code>in my view</code></p>
<ul>
<li><p>基础</p>
<ul>
<li>背景（optional）</li>
<li>改写两种观点 <code>Some people think that..., while others believe that</code></li>
</ul>
</li>
<li><p>进阶</p>
<ul>
<li>背景（optional）</li>
<li>概括争论点<code>People have different views about ...</code>，此时在两个主体段可以分别写 <code>Some people think that...,</code> <code>Others, however, believe that...</code></li>
</ul>
</li>
</ul>
<h2 id="比较利弊"><a href="#比较利弊" class="headerlink" title="比较利弊"></a>比较利弊</h2><ul>
<li>背景/观点</li>
<li>自己看法 <code>I believe that the advantage of this outweigh the disadvantages</code> , <code>I believe that the drawbacks of this outweigh the benefits</code></li>
</ul>
<h2 id="report-分析解释类"><a href="#report-分析解释类" class="headerlink" title="report(分析解释类)"></a>report(分析解释类)</h2><p><strong>仔细读题，看清楚分析什么</strong></p>
<p>eg. causes+solution </p>
<ul>
<li>开头<ul>
<li>改写现象</li>
<li>概括分析<ul>
<li><code>there are several causes of this trend</code></li>
<li><code>... and ... are respinsible for this trend</code></li>
</ul>
</li>
</ul>
</li>
<li>实质论述</li>
<li>解决办法<ul>
<li><code>various measures could be taken to solve the problem</code></li>
<li><code>a variety of measures could be taken to tackle the problem</code></li>
<li><code>the solution to the problem lies with ...</code></li>
</ul>
</li>
</ul>
<h2 id="结尾段"><a href="#结尾段" class="headerlink" title="结尾段"></a>结尾段</h2><p>1-2句话，<code>简要，概括，不偏题</code></p>
<h1 id="day5-分论点支持句"><a href="#day5-分论点支持句" class="headerlink" title="day5 分论点支持句"></a>day5 分论点支持句</h1><ul>
<li><p>支持句原则</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">idea -&gt; explain in detail -&gt; example</span><br></pre></td></tr></table></figure>

<p>为了避免机械重复，选择1-2个分论点举列子就可以了</p>
</li>
<li><p>思考方向</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">why (为什么) </span><br><span class="line">so  (有什么结果)</span><br><span class="line">what (这样的具体意思-&gt;分论点比较抽象的时候)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h1 id="day6-支持句变流畅-明暗结合"><a href="#day6-支持句变流畅-明暗结合" class="headerlink" title="day6 支持句变流畅(明暗结合)"></a>day6 支持句变流畅(明暗结合)</h1><ul>
<li><p>连接词（不要过多使用）</p>
<ul>
<li><p>因果 <code>therefore,</code>(常用在句首) <code>as a result,</code>(常用在句首)</p>
</li>
<li><p>举例 <code>for example,</code>(常用在句首)  <code>such as</code></p>
</li>
<li><p>对比展开 <code>while</code> <code>by contrast</code></p>
</li>
<li><p>类比展开 <code>similarly,</code>(常用在句首)</p>
</li>
<li><p>让步展开 <code>although</code></p>
</li>
<li><p>假设展开 <code>may</code> <code>are likely to</code> <code>without...,...would...</code></p>
</li>
<li><p>下定义展开 <code>This means that</code> <code>In other words,</code></p>
</li>
<li><p>限定范围展开 <code>Specifically,</code></p>
</li>
<li><p>连接分论点的连接词 <code>Also,</code> <code>Furthermore</code> <code>Another benefit/advantage of ... is</code></p>
<p><strong>不用<code>moreover</code></strong>,显得呆板深沉，只有在大学正是论文中使用较多</p>
<p><strong>不用<code>besides</code></strong>,过于口语化</p>
</li>
</ul>
</li>
<li><p>代词，相关名词（eg. urban residents -&gt; city dwellers）</p>
</li>
</ul>
<h1 id="day7-phrases（略）"><a href="#day7-phrases（略）" class="headerlink" title="day7 phrases（略）"></a>day7 phrases（略）</h1><h1 id="day8-高分作文"><a href="#day8-高分作文" class="headerlink" title="day8 高分作文"></a>day8 高分作文</h1><ul>
<li><p>260-300 words</p>
</li>
<li><p>可单独使用<code>on the other hand</code></p>
</li>
<li><p>可以用<code>first</code> <code>second</code>来表示分论点顺序，但是更加自然的做法是</p>
<ul>
<li><code>the main benefit/advantage of...is that</code></li>
<li><code>the main problem is that</code></li>
<li><code>another disadvantage is that</code></li>
<li><code>also,</code></li>
<li><code>futhermore</code>（等同重要）</li>
<li><code>in addtion</code>（补充说明）</li>
</ul>
</li>
<li><p>题目中出现<code>best</code> ，只需要指出不足之处或者替代选择就可以</p>
</li>
<li><p>题目中出现<code>only</code>， 一般是先承认这种看法有一定的道理，再指出其他可能性</p>
</li>
</ul>
]]></content>
      <tags>
        <tag>雅思</tag>
      </tags>
  </entry>
  <entry>
    <title>雅思口语part2</title>
    <url>/2020/07/24/ielts-speaking2/</url>
    <content><![CDATA[<p><strong>注意</strong></p>
<ol>
<li>写时态</li>
<li>idea  （写大一点）</li>
<li>单复数</li>
<li>He/she</li>
</ol>
<a id="more"></a>

<h1 id="组织"><a href="#组织" class="headerlink" title="组织"></a>组织</h1><h1 id="人物"><a href="#人物" class="headerlink" title="人物"></a>人物</h1><h2 id="taught"><a href="#taught" class="headerlink" title="taught"></a>taught</h2><ul>
<li>He is my senior</li>
<li>of average height and build</li>
<li>wears glasses</li>
<li>role model</li>
<li>hard-working</li>
<li>kind and caring</li>
<li>always there when you need him</li>
<li>C Programming Language </li>
</ul>
<h2 id="jack-ma"><a href="#jack-ma" class="headerlink" title="jack ma"></a>jack ma</h2><ul>
<li>head of the Alibaba Group, including the b2b marketplace Alibaba, the shopping site Taobao and alipay</li>
<li>Ma became interested in the  English language as a young boy, and during his teens he worked as a guide for foreign tourists to Hangzhou. Ma failed the entrance exam for the Hangzhou <a href="https://www.britannica.com/topic/normal-school" target="_blank" rel="noopener">Teachers College</a> twice. (His weak point was mathematics.) He was admitted on the third try</li>
<li>On a trip to the <a href="https://www.britannica.com/place/United-States" target="_blank" rel="noopener">United States</a> on behalf of the Hangzhou city government in 1995, Ma had his first encounter with the <a href="https://www.britannica.com/technology/Internet" target="_blank" rel="noopener">Internet</a> and saw the lack of Chinese Web sites as a great business opportunity</li>
<li>which launched a <a href="https://www.britannica.com/technology/Web-site" target="_blank" rel="noopener">Web site</a> that <a href="https://www.merriam-webster.com/dictionary/facilitated" target="_blank" rel="noopener">facilitated</a> deals between small businesses</li>
<li>Ma created a new company, the consumer-to-consumer online marketplace Taobao</li>
<li>see through the customer’s eye</li>
<li>“giving up is the greatest failure”</li>
<li>Jack Ma Foundation, Alibaba donate medical supplies, COVID-19 test kits to India, other nations</li>
<li>health care, education and the environment</li>
<li>He has a slim body with black hair</li>
</ul>
<h2 id="huiruoqi"><a href="#huiruoqi" class="headerlink" title="huiruoqi"></a>huiruoqi</h2><ul>
<li>former female Chinese <a href="https://en.wikipedia.org/wiki/Volleyball" target="_blank" rel="noopener">volleyball</a> player</li>
<li>She is the Outside Hitter and the fifteenth Captain of <a href="https://en.wikipedia.org/wiki/China_women' target="_blank" rel="noopener"s_national_volleyball_team">China women’s national volleyball team</a></li>
<li>She retired in 2018.</li>
<li>Olympic Games, World Cup, and world championship</li>
<li>“never say die” spirit</li>
<li>public service activity</li>
<li>strong in body and mind.</li>
<li>She had just undergone heart surgery, and seven steel nails were punched into her left shoulder, but she still stood on the court</li>
<li>launched a fund, hoping to help the retired athletes and coaches who have difficulties in medical treatment and life, and let more people pay attention to athletes and sports itself.</li>
</ul>
<h1 id="adj"><a href="#adj" class="headerlink" title="adj"></a>adj</h1><ul>
<li>terrific</li>
<li>delighted</li>
<li>thankful</li>
<li>proud</li>
<li>joyful</li>
<li>motivated</li>
<li>encouraged</li>
<li>amazed</li>
<li>touched</li>
<li>exhausted</li>
<li>irritated</li>
<li>disappointed</li>
</ul>
<h1 id="hot-pot"><a href="#hot-pot" class="headerlink" title="hot-pot"></a>hot-pot</h1><ul>
<li>is a chinese food</li>
<li>While a hot pot full of soup is kept boiling, raw ingredients are placed into the pot </li>
<li>Most raw foods can be cooked in a hot pot, although they may have different cooking times</li>
<li>The cooked food is often eaten with a dipping <a href="https://en.wikipedia.org/wiki/Sauce" target="_blank" rel="noopener">sauce</a> for additional flavoring.</li>
<li>Hot pots may be prepared and eaten either at home or in a restaurant. Since it is considered as a main dish, it can be enjoyed without other separate courses like rice or noodles. </li>
</ul>
<h2 id="how-to-eat"><a href="#how-to-eat" class="headerlink" title="how to eat"></a>how to eat</h2><ul>
<li>Choose the soup base </li>
<li>Make your own dipping sauce</li>
<li>cook food in a hot pot and eat. The best order of eating hot pot is to eat meat first, then vegetables</li>
</ul>
<h2 id="why-popular"><a href="#why-popular" class="headerlink" title="why popular"></a>why popular</h2><ul>
<li><p>convenient</p>
</li>
<li><p>enhance friendship and unite family members or colleagues as several people sit around a pot, talking and eating</p>
</li>
<li><p>The warm atmosphere makes people feel comfortable and relaxed.<a href="https://en.wikipedia.org/wiki/Hot_pot#cite_note-2" target="_blank" rel="noopener">[2]</a> Hot pot is especially popular in winter and cold places.</p>
</li>
</ul>
<h1 id="monopoly"><a href="#monopoly" class="headerlink" title="monopoly"></a>monopoly</h1><ul>
<li>a board game</li>
<li>In the game, players <code>roll</code> two six-sided <code>dice</code> to move around the game board, </li>
<li>buying and trading properties, and developing them with houses and hotels. </li>
<li>Players collect rent from their opponents, with the goal being to <code>drive them into bankruptcy</code>. </li>
<li>Money can also be gained or lost through Chance and destiny cards; </li>
<li>players can end up in jail, which they cannot move from until they have met one of several conditions. </li>
</ul>
<h1 id="Czech-tʃek-plague"><a href="#Czech-tʃek-plague" class="headerlink" title="Czech[tʃek] - plague"></a>Czech[tʃek] - plague</h1><ul>
<li>the capital of the Czech Republic</li>
<li>warm summers and chilly winters.</li>
<li>long history</li>
<li>one of the world’s most popular tourist destinations</li>
<li>Prague is home to a number of well-known cultural attractions</li>
<li>Prague Castle, Charles Bridge, Old Town Square</li>
<li>Spectacular architecture</li>
<li>stunning</li>
<li>Pork Knee</li>
</ul>
<h1 id="others"><a href="#others" class="headerlink" title="others"></a>others</h1><ul>
<li>parrot</li>
<li>fridge magnet</li>
<li>play house 过家家</li>
</ul>
]]></content>
      <tags>
        <tag>雅思</tag>
      </tags>
  </entry>
  <entry>
    <title>雅思小作文</title>
    <url>/2020/07/20/ielts_writing2/</url>
    <content><![CDATA[<h1 id="intro"><a href="#intro" class="headerlink" title="intro"></a>intro</h1><a id="more"></a>

<h2 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h2><ul>
<li><p>table</p>
</li>
<li><p>line graph</p>
</li>
<li><p>bar chart</p>
</li>
<li><p>pie chart </p>
</li>
<li><p>process diagram（单独）</p>
</li>
<li><p>map（单独）</p>
</li>
</ul>
<h2 id="要求"><a href="#要求" class="headerlink" title="要求"></a>要求</h2><ul>
<li>客观性</li>
<li>准确性（特征点没有落在准备坐标值时，用<code>about</code> ，<code>around</code>， <code>just under/above</code> 来估计）</li>
<li>详尽性</li>
<li>主题部分一般写2-3段，6-8句话</li>
</ul>
<h2 id="非正式表达"><a href="#非正式表达" class="headerlink" title="非正式表达"></a>非正式表达</h2><ul>
<li>boss-&gt;employer</li>
<li>bad -&gt; harmful</li>
<li>get-&gt;obtain</li>
<li>let you -&gt; enable you</li>
<li>enough -&gt; adequate</li>
</ul>
<h2 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h2><ul>
<li>5 million 不加 s</li>
<li>不能把rise用成arise（arise是指某种现象的发生）</li>
<li>不能把下降用成reduce</li>
<li>the figure for (不是of)</li>
<li>fall (fell)</li>
<li>关于时态<ul>
<li>‘此图展示了’ 等永恒事实用一般现在时</li>
<li>shows/illustrates that 后面根据具体时间来确定时态</li>
<li>题目里没有出现时间，全文用一般现在时</li>
</ul>
</li>
</ul>
<h1 id="开头段（改写）"><a href="#开头段（改写）" class="headerlink" title="开头段（改写）"></a>开头段（改写）</h1><ul>
<li><code>below</code> <code>above</code> 必须删除</li>
<li><code>show</code> - <code>illustrate</code> - <code>compare</code> (两个及以上种类) </li>
<li><code>gragh</code> - <code>line graph</code> ; <code>chart</code> - <code>bar chart</code> ； <code>process diagram/map</code>-&gt;<code>diagram</code></li>
<li><code>UK</code> -<code>Britain</code> - <code>the United Kingdom</code></li>
<li><code>information</code> - <code>data</code> (no s)</li>
<li><code>males</code> - <code>men</code> ; <code>females</code>-<code>women</code></li>
<li><code>from ... to ...</code> - <code>between ... and ...</code>- <code>over a period of ... years</code></li>
<li><code>subway system</code> - <code>underground railway system</code> - <code>underground train system</code></li>
<li><code>store</code> -<code>shop</code></li>
<li><code>the number of + C</code>- <code>the figure for</code></li>
<li><code>the amount of + U</code> - <code>the figure for</code></li>
<li><code>diffrent kinds of</code>  - 具体的</li>
<li><code>country</code> - <code>nation</code></li>
<li><code>sales of</code> - <code>sales date for</code></li>
<li><code>in 1999, 2003, 2005</code> - <code>in three separate years:1999, 2003, 2005</code> </li>
<li><code>transport</code> - <code>deliver</code></li>
</ul>
<h1 id="介绍数据和变化趋势"><a href="#介绍数据和变化趋势" class="headerlink" title="介绍数据和变化趋势"></a>介绍数据和变化趋势</h1><h2 id="动词"><a href="#动词" class="headerlink" title="动词"></a>动词</h2><h3 id="上升"><a href="#上升" class="headerlink" title="上升"></a>上升</h3><ul>
<li><code>go up</code>  <code>,up 40%</code></li>
<li><code>increase</code>  <code>show an increase of</code></li>
<li><code>rise (rose)</code>  <code>,rising to</code></li>
<li><code>grow</code></li>
<li><code>show an upward/downward trend</code></li>
<li><code>climb to</code></li>
</ul>
<h3 id="下降"><a href="#下降" class="headerlink" title="下降"></a>下降</h3><ul>
<li><code>decline</code>(不能用被动)</li>
<li><code>fall (fell)</code></li>
<li><code>drop (dropped)</code>   <code>,dropping to</code></li>
<li><code>decrease</code>  <code>show an decrease of</code></li>
<li><code>fall (fell) steadily</code></li>
<li><code>fall (fell) back to the 1980 figure</code></li>
<li><code>, down 35%</code></li>
</ul>
<table>
<thead>
<tr>
<th></th>
<th>语境</th>
<th>例子</th>
</tr>
</thead>
<tbody><tr>
<td>by</td>
<td>上升下降的幅度</td>
<td>by 2%</td>
</tr>
<tr>
<td>from … to …</td>
<td></td>
<td>incease from A to B</td>
</tr>
<tr>
<td>at</td>
<td>主语是对象；跟在所指代的数据后</td>
<td>at 20%</td>
</tr>
<tr>
<td>with</td>
<td>形容很大的数字</td>
<td>…, with 35 quadrillion units</td>
</tr>
<tr>
<td>to</td>
<td>上升下降到多少</td>
<td>it decreased to 250 grams</td>
</tr>
<tr>
<td>()</td>
<td>主语不是对象词</td>
<td>A with the highest percentage of land (23%)</td>
</tr>
</tbody></table>
<ul>
<li><code>saw an upward/downward trend</code></li>
<li><code>climb to</code>  <code>drop to</code></li>
</ul>
<h3 id="波动"><a href="#波动" class="headerlink" title="波动"></a>波动</h3><ul>
<li><code>fluctuate</code> (不能用被动)</li>
</ul>
<p>常用 <code>fluctuated between ... and ...</code></p>
<h3 id="稳定在某一水平"><a href="#稳定在某一水平" class="headerlink" title="稳定在某一水平"></a>稳定在某一水平</h3><ul>
<li><code>remain stable at</code>+ 数字/百分比</li>
<li><code>level off at</code> + 数字/百分比</li>
<li><code>stood at this level until ...</code></li>
<li><code>remained at this level in the rest of the period</code></li>
</ul>
<h3 id="经历了某种变化（时间变化图）"><a href="#经历了某种变化（时间变化图）" class="headerlink" title="经历了某种变化（时间变化图）"></a>经历了某种变化（时间变化图）</h3><ul>
<li><code>see (saw)</code></li>
<li><code>experience</code></li>
<li><code>witness (witnessed)</code><ul>
<li>London <code>saw a significant increase</code> in …</li>
<li>the cost <code>saw an opposite trend</code></li>
<li>the rate <code>experienced a sharp rise</code></li>
</ul>
</li>
</ul>
<h3 id="达到最高点"><a href="#达到最高点" class="headerlink" title="达到最高点"></a>达到最高点</h3><ul>
<li><code>peak at</code> + 数字/百分比</li>
<li><code>reach its peak at</code> + 数字/百分比</li>
</ul>
<h3 id="达到最低点"><a href="#达到最低点" class="headerlink" title="达到最低点"></a>达到最低点</h3><ul>
<li><code>reach its lowest point at</code></li>
<li><code>fall to its lowest point at</code></li>
</ul>
<h3 id="达到-位于"><a href="#达到-位于" class="headerlink" title="达到/位于"></a>达到/位于</h3><ul>
<li><code>reach</code></li>
<li><code>stand at (stood at)</code></li>
</ul>
<h3 id="所占比重（pie-chart）"><a href="#所占比重（pie-chart）" class="headerlink" title="所占比重（pie chart）"></a>所占比重（pie chart）</h3><ul>
<li><code>account for</code></li>
<li><code>make up</code></li>
<li><code>represent</code></li>
<li><code>constitute</code><ul>
<li>the cost of equipment represented 23% of the school budget.</li>
</ul>
</li>
</ul>
<h3 id="预测"><a href="#预测" class="headerlink" title="预测"></a>预测</h3><ul>
<li><code>expext (is expected to)</code></li>
<li><code>predict (is predicted to)</code></li>
<li><code>project （is projected to）</code></li>
</ul>
<p><strong>虽然是预估，但是仍然要用一般现在时</strong></p>
<h3 id="变为原来的n倍-动态"><a href="#变为原来的n倍-动态" class="headerlink" title="变为原来的n倍(动态)"></a>变为原来的n倍(动态)</h3><ul>
<li><code>double (doubled)  /  triple (tripled)</code></li>
<li><code>increase twofold / threefold</code><ul>
<li>Sales doubled from A to B</li>
<li>Sales tripled beween 1999 to 2000</li>
</ul>
</li>
<li><code>almost doubled</code></li>
<li><code>more than doubled</code></li>
<li><code>incease fivefold to 25% from . to .</code></li>
</ul>
<h3 id="是另一个的n倍（静态）"><a href="#是另一个的n倍（静态）" class="headerlink" title="是另一个的n倍（静态）"></a>是另一个的n倍（静态）</h3><ul>
<li><code>be twice (three times) as high as ...</code></li>
<li><code>was nearly three times as high as</code></li>
</ul>
<h3 id="始终高于-低于"><a href="#始终高于-低于" class="headerlink" title="始终高于/低于"></a>始终高于/低于</h3><ul>
<li><code>is consistently higher/lower than</code></li>
</ul>
<h3 id="原有的差距变小-加大"><a href="#原有的差距变小-加大" class="headerlink" title="原有的差距变小/加大"></a>原有的差距变小/加大</h3><ul>
<li><code>the gap between ... and ... narrows/ widens</code></li>
</ul>
<h3 id="原本少于后来反超"><a href="#原本少于后来反超" class="headerlink" title="原本少于后来反超"></a>原本少于后来反超</h3><ul>
<li><code>exceed</code> + 数字/百分比</li>
<li><code>overtake</code> + 对象</li>
</ul>
<h3 id="减少后恢复"><a href="#减少后恢复" class="headerlink" title="减少后恢复"></a>减少后恢复</h3><ul>
<li>…, although it <code>climbed back to</code></li>
</ul>
<h2 id="形容词和副词"><a href="#形容词和副词" class="headerlink" title="形容词和副词"></a>形容词和副词</h2><p><code>there was a remarkable change in the percentage of</code></p>
<p><code>saw a significant rise</code> </p>
<p><code>decreased sharply to</code></p>
<h3 id="急剧"><a href="#急剧" class="headerlink" title="急剧"></a>急剧</h3><ul>
<li><code>sharp</code></li>
<li><code>rapid</code></li>
<li><code>dramatic</code>(<code>dramatically</code>)</li>
</ul>
<h3 id="持续"><a href="#持续" class="headerlink" title="持续"></a>持续</h3><ul>
<li><p><code>steady</code> (稳步地)</p>
</li>
<li><p><code>gradual</code>(<code>gradually</code>)  （稳步地）</p>
</li>
<li><p><code>consistent</code> （稳步地/ 急剧的）</p>
</li>
</ul>
<h3 id="显著的-大幅度的"><a href="#显著的-大幅度的" class="headerlink" title="显著的/大幅度的"></a>显著的/大幅度的</h3><ul>
<li><code>significant</code></li>
<li><code>noticeable</code></li>
<li><code>considerable</code> (<code>considerably</code>)<ul>
<li>the rise was<code>particularly noticeable</code>bewteen A and B</li>
</ul>
</li>
</ul>
<h3 id="小幅度"><a href="#小幅度" class="headerlink" title="小幅度"></a>小幅度</h3><ul>
<li><code>slight</code> (<code>slightly</code>)</li>
</ul>
<h3 id="大约"><a href="#大约" class="headerlink" title="大约"></a>大约</h3><ul>
<li><code>about</code></li>
<li><code>around</code></li>
<li><code>just over/under</code></li>
<li><code>approximately</code></li>
</ul>
<h3 id="分别的"><a href="#分别的" class="headerlink" title="分别的"></a>分别的</h3><ul>
<li><code>respectively</code><ul>
<li>in A and B, sales grew from a to b and from a to b <code>respectively</code></li>
</ul>
</li>
</ul>
<h3 id="相同或者相反趋势"><a href="#相同或者相反趋势" class="headerlink" title="相同或者相反趋势"></a>相同或者相反趋势</h3><ul>
<li><code>..., while there is a different pattern in ...</code></li>
<li><code>a totally different pattern is expected to be seen in</code></li>
<li><code>there will be a similar trend in</code></li>
<li><code>a similar trend was seen in</code></li>
<li><code>the rising was also seen in</code></li>
<li><code>similar changes happened to ....</code></li>
</ul>
<h2 id="数字"><a href="#数字" class="headerlink" title="数字"></a>数字</h2><table>
<thead>
<tr>
<th>百分比</th>
<th>分数1</th>
<th>分数2</th>
</tr>
</thead>
<tbody><tr>
<td>90%</td>
<td>the vast majority of</td>
<td>nine out of ten</td>
</tr>
<tr>
<td>75%</td>
<td>three <strong>quaters</strong> of</td>
<td>nearly eight out of ten</td>
</tr>
<tr>
<td>66%</td>
<td>two <strong>thirds</strong> of</td>
<td>six <strong>out of</strong> ten</td>
</tr>
<tr>
<td>10%</td>
<td>one <strong>tenth</strong> of</td>
<td>one <strong>in</strong> ten</td>
</tr>
</tbody></table>
<h1 id="主体段（1-X）"><a href="#主体段（1-X）" class="headerlink" title="主体段（1+X）"></a>主体段（1+X）</h1><h2 id="分段"><a href="#分段" class="headerlink" title="分段"></a>分段</h2><table>
<thead>
<tr>
<th>情形</th>
<th>动态图</th>
<th>静态图</th>
</tr>
</thead>
<tbody><tr>
<td>单图，2-3对象</td>
<td>对象</td>
<td>对象</td>
</tr>
<tr>
<td>单图，多对象</td>
<td>高低/趋势/性质(developing/developed)</td>
<td>高低/性质</td>
</tr>
<tr>
<td>多图，对象单位一致</td>
<td>高低/趋势/性质</td>
<td>高低/趋势/性质</td>
</tr>
<tr>
<td>多图，对象单位不一样</td>
<td>一幅图一段</td>
<td>一幅图一段</td>
</tr>
</tbody></table>
<h2 id="写前准备"><a href="#写前准备" class="headerlink" title="写前准备"></a>写前准备</h2><ol>
<li><p>时态（很重要）</p>
<p>又有过去又有预估，从预估到过去</p>
<ul>
<li>the line graph shows the energy consumed in US from . to ., <code>as well as projected figures for the year</code> 2030</li>
<li>… are projected to …at 2050, <code>up from</code> 46% in 2000.</li>
<li>…, significantly higher than 24% in 2020</li>
</ul>
</li>
<li><p>对象/对象词</p>
<ul>
<li><code>the number/amount of</code></li>
<li><code>the figure for</code></li>
<li>定从/短语/…</li>
</ul>
</li>
<li><p>分段</p>
</li>
<li><p>总结</p>
</li>
</ol>
<h2 id="整体概括（1）"><a href="#整体概括（1）" class="headerlink" title="整体概括（1）"></a>整体概括（1）</h2><ul>
<li>动态图<ul>
<li>整体变化趋势（上升，下降，波动，平稳）<ul>
<li>the tables shows that the sales <code>increased in all five countries</code>.</li>
</ul>
</li>
<li>哪一类数据整体高于其他类<ul>
<li>it is clear that the A in Britain is <code>significantly higher than</code> other countries.</li>
</ul>
</li>
</ul>
</li>
<li>静态图<ul>
<li>包含哪几类数据<ul>
<li>the pie chart shows that there are <code>four main causes</code> of farmland becoming degraded in the world.</li>
</ul>
</li>
<li>哪一类数据整体高于其他类<ul>
<li>the table shows that Europe <code>had the highest propotion</code> of A among the three regions in the 1990s</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>第二段的开头有对比用</p>
<p><code>in contrast</code>  意思是对照之下、与……成对比。强调形成对比、对照。</p>
<p><strong>开头第一句</strong></p>
<ul>
<li><p>直接开始说</p>
</li>
<li><p><code>it is clear from the (kinds) that ...</code></p>
</li>
<li><p><code>we can see from the (kinds) that</code></p>
</li>
<li><p><code>it can be seen from the (kinds) that</code></p>
</li>
<li><p><code>the (kinds) shows that</code></p>
</li>
</ul>
<h3 id="一定出现的数据特征"><a href="#一定出现的数据特征" class="headerlink" title="一定出现的数据特征"></a>一定出现的数据特征</h3><ul>
<li>起点/终点 （动态图）</li>
<li>变化趋势</li>
<li>最大值/最小值</li>
<li>等值 </li>
<li>倍数关系</li>
</ul>
<h3 id="技巧"><a href="#技巧" class="headerlink" title="技巧"></a>技巧</h3><ul>
<li>类比 <code>similarly</code></li>
<li>对比<code>saw an oppssite trend</code></li>
</ul>
<h2 id="积累"><a href="#积累" class="headerlink" title="积累"></a>积累</h2><ul>
<li><code>despite the growth</code>, it was the least popular means of transport</li>
<li>although no figure was given for / with no figure provided for the year 1980</li>
<li>by comparison (拼写)</li>
<li>gender differences</li>
<li>see the biggest gender gap</li>
<li>drop with age</li>
</ul>
<h2 id="静态图补充"><a href="#静态图补充" class="headerlink" title="静态图补充"></a>静态图补充</h2><p>数据更经常用括号补充</p>
<h3 id="比较"><a href="#比较" class="headerlink" title="比较"></a>比较</h3><p>静态图中涉及很多比较，且要多方向的比较避免写不够</p>
<ul>
<li>is twice as high as the figure for …</li>
<li>is twice higher than that of </li>
<li>…, higher than the figure for</li>
<li>is different from that of </li>
<li>…, compared with 4% of women</li>
<li>by comparison, …</li>
</ul>
<h3 id="关于年龄说法"><a href="#关于年龄说法" class="headerlink" title="关于年龄说法"></a>关于年龄说法</h3><ul>
<li><code>people aged</code> under 24</li>
<li>people <code>in their thirties</code></li>
<li>under-24 <code>age group</code></li>
<li>under-24<code>-year-old</code> group</li>
</ul>
<h1 id="结尾段"><a href="#结尾段" class="headerlink" title="结尾段"></a>结尾段</h1><p><code>Overall</code></p>
<p><code>in summary</code></p>
<p><code>to summarise</code></p>
<p><code>... is expected to rise to varying degrees, while ... will show a different pattern</code></p>
<ol>
<li><p>简单概括下整体趋势，甚至可以说ABC之间存在着明显的差异</p>
<p>in summary, A in different cities vary significantly in A, B, and C.</p>
</li>
<li><p>不要出现数据</p>
</li>
<li><p>不要揣测数据变化的原因，不要给建议</p>
</li>
</ol>
<h1 id="流程图"><a href="#流程图" class="headerlink" title="流程图"></a>流程图</h1><h2 id="intro-1"><a href="#intro-1" class="headerlink" title="intro"></a>intro</h2><ul>
<li>仔细留意每一个细节</li>
<li>生词照抄</li>
<li>多用表示顺序的单词短语</li>
<li>流程图有很多小图-&gt; 分成几个大的阶段</li>
<li>多写字，多写细节，不然字数不够</li>
<li>工序图不要 overuse the provided labels in the diagram, 可以paraphrasing或者描述 <code>in something that looks like a cylinder</code></li>
<li>注意题目中的 compare</li>
<li>写前明确时态，单复数</li>
</ul>
<h2 id="分类-1"><a href="#分类-1" class="headerlink" title="分类"></a>分类</h2><table>
<thead>
<tr>
<th></th>
<th>工序图</th>
<th>生物生长图</th>
</tr>
</thead>
<tbody><tr>
<td>语态</td>
<td>被动语态为主</td>
<td>主动语态为主</td>
</tr>
<tr>
<td>对比</td>
<td>complex/simple  below/above the ground</td>
<td>大小/行为</td>
</tr>
<tr>
<td>分段</td>
<td>两段，中间断开</td>
<td>两段，中间断开</td>
</tr>
<tr>
<td>总结</td>
<td>步骤数量/规律特征/对比</td>
<td>步骤数量/规律特征/对比</td>
</tr>
</tbody></table>
<h2 id="相关词语"><a href="#相关词语" class="headerlink" title="相关词语"></a>相关词语</h2><h3 id="工序图"><a href="#工序图" class="headerlink" title="工序图"></a>工序图</h3><table>
<thead>
<tr>
<th>收集</th>
<th>collect,gather,harvest</th>
</tr>
</thead>
<tbody><tr>
<td>传送</td>
<td>deliver, send, transfer, tranport, feed(feed to)</td>
</tr>
<tr>
<td>转化</td>
<td>convert into, transform into</td>
</tr>
<tr>
<td>混合</td>
<td>mix,blend</td>
</tr>
<tr>
<td>使用</td>
<td>use,apply,utilise</td>
</tr>
<tr>
<td>扔掉</td>
<td>remove,throw away</td>
</tr>
<tr>
<td>生产，制造</td>
<td>produce, generate, create,make</td>
</tr>
<tr>
<td>加热，弄干，降温</td>
<td>heat, dry, cool, freeze</td>
</tr>
<tr>
<td>压缩</td>
<td>compress,condense</td>
</tr>
<tr>
<td>加工</td>
<td>process</td>
</tr>
<tr>
<td>装罐，包装</td>
<td>can, pack（捆在一起）, package（包装）（is packaged <code>in</code> bags）</td>
</tr>
<tr>
<td>经过</td>
<td>go through / pass through</td>
</tr>
<tr>
<td>成品</td>
<td>final product / end product</td>
</tr>
<tr>
<td>碾压grinder (a heavy roller)</td>
<td>grind (ground into)</td>
</tr>
<tr>
<td>原料</td>
<td>begins with the raw materials</td>
</tr>
<tr>
<td>转动</td>
<td>rotates in anticlockwise direction</td>
</tr>
<tr>
<td>传送带</td>
<td>send to / falls onto conveyor belt</td>
</tr>
</tbody></table>
<ul>
<li>compound 混合物</li>
<li>create <code>a</code> powder</li>
<li>the first step of … production involves</li>
<li>horizontally/vertically</li>
</ul>
<h3 id="生物生长动词"><a href="#生物生长动词" class="headerlink" title="生物生长动词"></a>生物生长动词</h3><table>
<thead>
<tr>
<th>产卵</th>
<th>lay eggs, produce eggs, deposit eggs</th>
</tr>
</thead>
<tbody><tr>
<td>孵化</td>
<td>hatch eggs, incubate eggs</td>
</tr>
<tr>
<td>破卵而出</td>
<td>break the shell, come out of eggs, hatch from eggs, egg breaks, egg cracks</td>
</tr>
<tr>
<td>变大</td>
<td>grow older, is reared</td>
</tr>
<tr>
<td>成年</td>
<td>reach maturity, grow to maturity, come to maturity, reach adulthood,fully matured</td>
</tr>
<tr>
<td>描述长度</td>
<td>measure 3 centimeter / 3 centimeter in length / twice as much as that of …</td>
</tr>
</tbody></table>
<h3 id="生物生长的时间"><a href="#生物生长的时间" class="headerlink" title="生物生长的时间"></a>生物生长的时间</h3><ul>
<li><code>for ...</code></li>
<li><code>it takes ten days for sb to</code></li>
<li><code>this stage lasts for</code> up to six weeks</li>
<li><code>after a period of</code>about three weeks</li>
<li><code>during a periof of ...</code></li>
<li>and <code>the life circle begins again</code> (循环往复)</li>
</ul>
<h2 id="顺序词"><a href="#顺序词" class="headerlink" title="顺序词"></a>顺序词</h2><ul>
<li><p>第一阶段</p>
<ul>
<li><code>at the first stage of the process</code></li>
<li><code>the first/initial step/phase</code></li>
<li><code>first</code> /<code>first of all</code></li>
<li><code>to begin with</code> / <code>the process begins with the +(n.)</code></li>
<li><code>at the larval stage</code> (在幼虫阶段) （<code>life stages</code> 生命阶段）</li>
</ul>
</li>
<li><p>下一阶段</p>
<ul>
<li><code>at(in) the next/second/third/fourth stage/phase</code></li>
<li><code>in(at) the following stage</code></li>
<li><code>reach/enter the next stage</code></li>
<li><code>mark/represent the ... stage</code></li>
<li><code>at this stage/point,</code></li>
<li><code>after this/that</code></li>
<li><code>next</code> / <code>then</code> / <code>afterwards</code> / <code>subsequently</code>(随后的)</li>
</ul>
</li>
<li><p>最后</p>
<ul>
<li>finally, </li>
<li>eventually, (除非很曲折，否则不要用)</li>
<li>at the adult/mature stage (成熟的阶段)</li>
</ul>
</li>
<li><p>总结</p>
<ul>
<li>go/pass through three stages</li>
<li>consis of three stages</li>
</ul>
</li>
</ul>
<h1 id="地图（map）"><a href="#地图（map）" class="headerlink" title="地图（map）"></a>地图（map）</h1><h2 id="intro-2"><a href="#intro-2" class="headerlink" title="intro"></a>intro</h2><p>map -&gt; diagram / image / picture</p>
<h3 id="分类-2"><a href="#分类-2" class="headerlink" title="分类"></a>分类</h3><ul>
<li>大地图</li>
<li>小地图/建筑内部图</li>
</ul>
<table>
<thead>
<tr>
<th></th>
<th>大地图</th>
<th>小地图/建筑内部图</th>
</tr>
</thead>
<tbody><tr>
<td>起始点</td>
<td>主要建筑，然后从西到东/顺时针</td>
<td>入口</td>
</tr>
</tbody></table>
<p>分段：</p>
<ul>
<li>时间</li>
</ul>
<table>
<thead>
<tr>
<th></th>
<th>过去</th>
<th>现在</th>
<th>未来</th>
</tr>
</thead>
<tbody><tr>
<td>动词</td>
<td></td>
<td>have been</td>
<td>is planned/expected/set/proposed to</td>
</tr>
<tr>
<td>形容词</td>
<td>old/past/original</td>
<td>current/existing/present</td>
<td>future/planned/proposed</td>
</tr>
<tr>
<td>状语</td>
<td>in the past</td>
<td>currently,now,at present</td>
<td>in the future, according to the plan, as planned</td>
</tr>
</tbody></table>
<ul>
<li><p>区域（大地图可以按照南北，东西分段）</p>
</li>
<li><p>性质（核心建筑/辅助设施）</p>
</li>
</ul>
<h2 id="词语"><a href="#词语" class="headerlink" title="词语"></a>词语</h2><h3 id="动词-1"><a href="#动词-1" class="headerlink" title="动词"></a>动词</h3><table>
<thead>
<tr>
<th>位于</th>
<th>located, situated</th>
</tr>
</thead>
<tbody><tr>
<td>连接</td>
<td>link with, connect, join</td>
</tr>
<tr>
<td>面积变大</td>
<td>extended</td>
</tr>
<tr>
<td>面积变小</td>
<td>reduced in size</td>
</tr>
<tr>
<td>某地点被去掉/取代</td>
<td>replaced, demolished,removed, displaced</td>
</tr>
<tr>
<td>某地点转移</td>
<td>relocated</td>
</tr>
<tr>
<td>新的建筑物</td>
<td>build/construct/developed/created/added/proposed</td>
</tr>
</tbody></table>
<h3 id="介词"><a href="#介词" class="headerlink" title="介词"></a>介词</h3><table>
<thead>
<tr>
<th>in</th>
<th>里面</th>
</tr>
</thead>
<tbody><tr>
<td>on</td>
<td>边上</td>
</tr>
<tr>
<td>to</td>
<td>外面</td>
</tr>
<tr>
<td>near, next to, close to, adjacent to</td>
<td>附近</td>
</tr>
<tr>
<td>within</td>
<td>里面</td>
</tr>
<tr>
<td>at</td>
<td>具体地方</td>
</tr>
<tr>
<td>outside</td>
<td>外面（outside of the building）</td>
</tr>
<tr>
<td>alongside</td>
<td>沿着</td>
</tr>
</tbody></table>
<h3 id="名词"><a href="#名词" class="headerlink" title="名词"></a>名词</h3><table>
<thead>
<tr>
<th>part</th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>edge/side</td>
<td><code>on</code> the edgy</td>
</tr>
<tr>
<td>wing</td>
<td>建筑物的一侧（<code>in</code> the west wing）</td>
</tr>
<tr>
<td>end / tip</td>
<td><code>on</code> the southern tip of this area</td>
</tr>
<tr>
<td>corner</td>
<td></td>
</tr>
<tr>
<td>center</td>
<td><code>in</code> the center of</td>
</tr>
<tr>
<td>front/back</td>
<td>at the front/back of</td>
</tr>
</tbody></table>
<h3 id="连接词"><a href="#连接词" class="headerlink" title="连接词"></a>连接词</h3><ul>
<li>one major developent, according to the plan, is…</li>
<li>another principle change is </li>
</ul>
<h3 id="积累-1"><a href="#积累-1" class="headerlink" title="积累"></a>积累</h3><ul>
<li>（开头段） <code>show the (proposed) changes to</code></li>
<li>（小地图开始）enter the center through the south entrance and see the … in front of …</li>
<li><code>man-made structure</code></li>
<li><code>key facilities</code></li>
<li>A is linked with B <code>by</code> a vehicle track</li>
<li>the road <code>runs through</code> the town <code>from west to east</code></li>
<li><code>pedestrianisation</code></li>
<li>ring road</li>
<li>are set to be demolished/removed to <code>make way for</code></li>
<li>to be reduced in size to<code>allow for</code>new housing development</li>
</ul>
<h3 id="句型"><a href="#句型" class="headerlink" title="句型"></a>句型</h3><ul>
<li><code>there be</code></li>
<li>倒装<ul>
<li><code>to the south of the park is a new apartment</code></li>
</ul>
</li>
<li>with<ul>
<li><code>the park remains with a new apartment building to the south</code></li>
</ul>
</li>
</ul>
]]></content>
      <tags>
        <tag>雅思</tag>
      </tags>
  </entry>
  <entry>
    <title>kaggle</title>
    <url>/2021/02/02/kaggle-intro%20to%20ML/</url>
    <content><![CDATA[<h1 id="kaggle-intro-to-ML"><a href="#kaggle-intro-to-ML" class="headerlink" title="kaggle-intro to ML"></a>kaggle-intro to ML</h1><h2 id="pandas-data"><a href="#pandas-data" class="headerlink" title="pandas - data"></a>pandas - data</h2><ul>
<li><p>read data</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line">data_file_path = <span class="string">'../../../data.csv'</span></span><br><span class="line">data = pd.read_csv(data_file_path)</span><br></pre></td></tr></table></figure>

<a id="more"></a>
</li>
<li><p>DataFrame –&gt; sheet in Excel</p>
</li>
<li><p>see data</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data.shape()  <span class="comment"># (row, column)</span></span><br><span class="line">data.describe()    <span class="comment"># count / mean / std / max / min ...</span></span><br><span class="line">data.head()    <span class="comment"># top 5 rows</span></span><br><span class="line">data.columns   <span class="comment"># columns names</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>select data</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data.columns   <span class="comment"># columns names</span></span><br><span class="line">data = data.dropna(axis=<span class="number">0</span>) <span class="comment"># drop not available (missing) data</span></span><br><span class="line"></span><br><span class="line">y = data.price <span class="comment"># select prediction target</span></span><br><span class="line"></span><br><span class="line">data_features = [<span class="string">'rooms'</span>, <span class="string">'size'</span>, <span class="string">'direction'</span>]  </span><br><span class="line">X = data[data_features]</span><br><span class="line">X.describe()  <span class="comment"># check</span></span><br><span class="line">X.head()</span><br></pre></td></tr></table></figure>

<ul>
<li><p>select prediction target</p>
<ul>
<li>select column –&gt; dot notation</li>
<li>single column is stored in <code>Series</code></li>
</ul>
</li>
<li><p>select features</p>
<ul>
<li>a list of column names –&gt; string with quotes</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="Sklearn-scikit-learn-Model"><a href="#Sklearn-scikit-learn-Model" class="headerlink" title="Sklearn (scikit-learn) - Model"></a>Sklearn (scikit-learn) - Model</h2><ul>
<li><p>modeling the types of data typically stored in DataFrames</p>
</li>
<li><p>steps</p>
<ul>
<li>define (model + parameters)</li>
<li>fit</li>
<li>predict</li>
<li>evaluate</li>
</ul>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DecisionTreeRegressor</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> mean_absolute_error</span><br><span class="line"></span><br><span class="line">train_X, val_X, train_y, val_y = train_test_split(X, y, random_state = <span class="number">0</span>) <span class="comment"># split test to train and validation</span></span><br><span class="line">house_model = DecisionTreeRegressor () <span class="comment"># define</span></span><br><span class="line">house_model.fit(train_X, train_y) <span class="comment"># fit</span></span><br><span class="line">val_prediciton = house_model.predict(val_X) <span class="comment"># predict</span></span><br><span class="line">error = mean_absolute_error(val_y, val_prediciton) <span class="comment">#evaluate</span></span><br><span class="line">print(error)</span><br></pre></td></tr></table></figure>

<ul>
<li><code>random_state</code> ensures you get the same results in each run, can use any number</li>
</ul>
</li>
<li><p>overfitting vs. underfitting by <code>max_leaf_nodes</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> mean_absolute_error</span><br><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DecisionTreeRegressor</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># Load data</span></span><br><span class="line">melbourne_file_path = <span class="string">'../input/melbourne-housing-snapshot/melb_data.csv'</span></span><br><span class="line">melbourne_data = pd.read_csv(melbourne_file_path) </span><br><span class="line"><span class="comment"># Filter rows with missing values</span></span><br><span class="line">filtered_melbourne_data = melbourne_data.dropna(axis=<span class="number">0</span>)</span><br><span class="line"><span class="comment"># Choose target and features</span></span><br><span class="line">y = filtered_melbourne_data.Price</span><br><span class="line">melbourne_features = [<span class="string">'Rooms'</span>, <span class="string">'Bathroom'</span>, <span class="string">'Landsize'</span>, <span class="string">'BuildingArea'</span>]</span><br><span class="line">X = filtered_melbourne_data[melbourne_features]</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_mae</span><span class="params">(max_leaf_nodes, train_X, val_X, train_y, val_y)</span>:</span></span><br><span class="line">    model = DecisionTreeRegressor(max_leaf_nodes=max_leaf_nodes, random_state=<span class="number">0</span>)</span><br><span class="line">    model.fit(train_X, train_y)</span><br><span class="line">    preds_val = model.predict(val_X)</span><br><span class="line">    mae = mean_absolute_error(val_y, preds_val)</span><br><span class="line">    <span class="keyword">return</span>(mae)</span><br><span class="line"></span><br><span class="line"><span class="comment"># split data into training and validation data, for both features and target</span></span><br><span class="line">train_X, val_X, train_y, val_y = train_test_split(X, y,random_state = <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># compare MAE with differing values of max_leaf_nodes</span></span><br><span class="line"><span class="keyword">for</span> max_leaf_nodes <span class="keyword">in</span> [<span class="number">5</span>, <span class="number">50</span>, <span class="number">500</span>, <span class="number">5000</span>]:</span><br><span class="line">    scores = &#123;leaf_size: get_mae(leaf_size, train_X, val_X, train_y, val_y) <span class="keyword">for</span> leaf_size <span class="keyword">in</span> candidate_max_leaf_nodes&#125;</span><br><span class="line">    </span><br><span class="line">best_tree_size = min(scores, key=scores.get)</span><br><span class="line"></span><br><span class="line">final_model = DecisionTreeRegressor(max_leaf_nodes = best_tree_size, random_state = <span class="number">0</span>)</span><br><span class="line">final_model.fit(X, y)</span><br><span class="line"></span><br><span class="line">test_data_path = <span class="string">'../input/test.csv'</span></span><br><span class="line">test_data = read_csv(test_data_path)</span><br><span class="line">test_X = test_data[features]</span><br><span class="line">test_preds = rf_model.predict(test_X)</span><br><span class="line"></span><br><span class="line"><span class="comment"># save predictions in format used for competition scoring</span></span><br><span class="line">output = pd.DataFrame(&#123;<span class="string">'Id'</span>: test_data.Id,</span><br><span class="line">                       <span class="string">'SalePrice'</span>: test_preds&#125;)</span><br><span class="line">output.to_csv(<span class="string">'submission.csv'</span>, index=<span class="literal">False</span>)</span><br></pre></td></tr></table></figure>

<ul>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">get(self, key, default=<span class="literal">None</span>, /)</span><br><span class="line">    Return the value <span class="keyword">for</span> key <span class="keyword">if</span> key <span class="keyword">is</span> <span class="keyword">in</span> the dictionary, <span class="keyword">else</span> default</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>random forest –&gt; many random trees and take average</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> RandomForestRegressor</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> mean_absolute_error</span><br><span class="line"></span><br><span class="line">forest_model = RandomForestRegressor(random_state=<span class="number">1</span>)</span><br><span class="line">forest_model.fit(train_X, train_y)</span><br><span class="line">melb_preds = forest_model.predict(val_X)</span><br><span class="line">print(mean_absolute_error(val_y, melb_preds))</span><br></pre></td></tr></table></figure>

</li>
</ul>
]]></content>
      <categories>
        <category>ML</category>
      </categories>
      <tags>
        <tag>Kaggle, ML</tag>
      </tags>
  </entry>
  <entry>
    <title>linux</title>
    <url>/2020/11/15/linux/</url>
    <content><![CDATA[<h1 id="Linux命令"><a href="#Linux命令" class="headerlink" title="Linux命令"></a><strong>Linux命令</strong></h1><h2 id="常见"><a href="#常见" class="headerlink" title="常见"></a>常见</h2><ul>
<li><code>pwd</code>（print working directory）：查看用户的当前目录</li>
<li><code>cd</code> ： 切换目录</li>
<li><code>.</code>表示当前目录</li>
<li><code>..</code> 表示当前目录的上一级目录（父目录）</li>
<li><code>-</code>表示用 cd 命令切换目录<strong>前</strong>所在的目录</li>
<li><code>~</code> 表示<strong>用户主目录</strong>的绝对路径名</li>
</ul>
<a id="more"></a>

<ul>
<li><strong>绝对路径：</strong>以斜线（/）开头 </li>
<li><strong>相对路径 ：</strong>不以斜线（/）开头</li>
</ul>
<h2 id="常用快捷键"><a href="#常用快捷键" class="headerlink" title="常用快捷键"></a>常用快捷键</h2><ul>
<li><code>ctrl+l</code> clear screen</li>
<li><code>ctrl+r</code>  查看历史命令</li>
<li><code>ctrl+q</code> quit</li>
</ul>
<h2 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h2><ul>
<li><p><code>rmdir</code>：要求目录为空</p>
</li>
<li><p><code>touch</code>：生成一个空文件或更改文件的时间</p>
</li>
<li><p><code>cp</code>：复制文件或目录</p>
</li>
<li><p><code>mv</code>：移动文件或目录、文件或目录改名</p>
</li>
<li><p><code>rm</code>：删除文件或目录</p>
</li>
<li><p><code>ln</code>：建立链接文件</p>
</li>
<li><p><code>find</code>：查找文件</p>
</li>
<li><p><code>file/stat</code>：查看文件类型或文件属性信息</p>
</li>
<li><p><code>cat：</code>查看文本文件内容</p>
</li>
<li><p><code>more：</code>可以分页看</p>
</li>
<li><p><code>less：</code>不仅可以分页，还可以方便地搜索，回翻等操作</p>
</li>
<li><p><code>tail -10</code>： 查看文件的尾部的10行</p>
</li>
<li><p><code>head -20</code>：查看文件的头部20行</p>
</li>
<li><p><code>echo</code>：把内容重定向到指定的文件中 ，有则打开，无则创建</p>
</li>
<li><p><code>管道命令 |</code> ：将前面的结果给后面的命令，例如：<code>ls -la | wc</code>，将ls的结果加油wc命令来统计字数</p>
</li>
</ul>
<ul>
<li><code>重定向 &gt; 是覆盖模式，&gt;&gt; 是追加模式</code>，例如：<code>echo &quot;Java3y,zhen de hen xihuan ni&quot; &gt; qingshu.txt</code>把左边的输出放到右边的文件里去</li>
</ul>
<h2 id="压缩-解压"><a href="#压缩-解压" class="headerlink" title="压缩/解压"></a>压缩/解压</h2><ul>
<li><code>gzip filename</code></li>
<li><code>bzip2 filename</code></li>
<li><code>tar -czvf filename</code></li>
</ul>
<ul>
<li><code>gzip -d filename.gz</code></li>
<li><code>bzip2 -d filename.bz2</code></li>
<li><code>tar -xzvf filename.tar.gz</code></li>
</ul>
<h1 id="linux基础"><a href="#linux基础" class="headerlink" title="linux基础"></a>linux基础</h1><h2 id="1-系统组成"><a href="#1-系统组成" class="headerlink" title="1.系统组成"></a>1.系统组成</h2><ol>
<li><p><strong>linux内核</strong>（linus 团队管理）</p>
</li>
<li><p><strong>shell</strong>：用户与内核交互的接口 </p>
<p>Shell可以执行：</p>
<ul>
<li><strong>内部命令</strong></li>
<li><strong>应用程序</strong></li>
<li><strong>shell脚本</strong></li>
</ul>
</li>
<li><p><strong>文件系统</strong>：ext3、ext4等。windows 有 fat32 、ntfs</p>
</li>
<li><p><strong>第三方应用软件</strong></p>
</li>
</ol>
<h2 id="2-Linux基本目录结构"><a href="#2-Linux基本目录结构" class="headerlink" title="2.Linux基本目录结构"></a>2.Linux基本目录结构</h2><p><img src="https://pic2.zhimg.com/v2-1f6cdbc3e0765ae8484624eaa2a08ab9_b.jpg" alt="img"></p>
<ul>
<li><strong>bin 存放二进制可执行文件(ls,cat,mkdir等)</strong>+</li>
<li>boot 存放用于系统引导时使用的各种文件+ </li>
<li>dev 用于存放设备文件</li>
<li><strong>etc 存放系统配置文件</strong><ul>
<li>home 存放所有用户文件的根目录</li>
<li>lib 存放跟文件系统中的程序运行所需要的共享库及内核模块</li>
</ul>
</li>
<li>mnt 系统管理员安装临时文件系统的安装点</li>
<li><strong>opt 额外安装的可选应用程序包所放置的位置</strong></li>
<li>proc 虚拟文件系统，存放当前内存的映射</li>
<li><strong>root 超级用户目录</strong></li>
<li>sbin 存放二进制可执行文件，只有root才能访问</li>
<li>tmp 用于存放各种临时文件</li>
<li>usr 用于存放系统应用程序，比较重要的目录/usr/local 本地管理员软件安装目录</li>
<li>var 用于存放运行时需要改变数据的文件</li>
</ul>
<h2 id="命令-cmd-options-arguments"><a href="#命令-cmd-options-arguments" class="headerlink" title="命令 cmd [options] [arguments]"></a>命令 cmd [options] [arguments]</h2><ul>
<li>Linux是<strong>区分大小</strong>写的</li>
<li><strong>单字符</strong>选项前使用<strong>一个</strong><code>减号-</code>。<strong>单词选项</strong>前使用两个<code>减号--</code></li>
<li><strong>通配符</strong><ul>
<li>*：匹配任何字符和任何数目的字符</li>
<li>?：匹配单一数目的任何字符</li>
<li>[ ]：匹配[ ]之内的任意一个字符</li>
<li>[! ]：匹配除了[! ]之外的任意一个字符，!表示非的意思</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>python基础</title>
    <url>/2020/11/19/python-basic/</url>
    <content><![CDATA[<h1 id="python基础"><a href="#python基础" class="headerlink" title="python基础"></a>python基础</h1><h2 id="from-kaggle"><a href="#from-kaggle" class="headerlink" title="from kaggle"></a>from kaggle</h2><ul>
<li><p>help()   : pass the name, not the result</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">help(round)</span><br><span class="line"><span class="comment"># help(round(3.0))</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>docstring：provide a description of a function</p>
<a id="more"></a>

<ul>
<li>triple-quoted string</li>
<li>after the header of a function</li>
<li>help() –&gt; shows the docstring</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">least_difference</span><span class="params">(a, b, c)</span>:</span></span><br><span class="line">    <span class="string">"""Return the smallest difference between any two numbers</span></span><br><span class="line"><span class="string">    among a, b and c.</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    &gt;&gt;&gt; least_difference(1, 5, -5)</span></span><br><span class="line"><span class="string">    4</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    diff1 = abs(a - b)</span><br><span class="line">    diff2 = abs(b - c)</span><br><span class="line">    diff3 = abs(a - c)</span><br><span class="line">    <span class="keyword">return</span> min(diff1, diff2, diff3)</span><br></pre></td></tr></table></figure>
</li>
<li><p>and 优先级 &gt; or</p>
</li>
<li><p>objects carry something, and the stuff is accessed using Python’s dot syntax</p>
<ul>
<li>carry: funciotn –&gt; method</li>
<li>carry: non-function –&gt; attributes</li>
</ul>
</li>
</ul>
<h3 id="list"><a href="#list" class="headerlink" title="list"></a>list</h3><ul>
<li><p>lists (mutable)</p>
<ul>
<li><p>indexing <code>list[0]</code></p>
</li>
<li><p>slicing <code>list[0:3]</code>   <code>list[-1:1]</code> </p>
</li>
<li><p>change <code>list[1]=&quot;second&quot;</code></p>
</li>
<li><p>list comprehensions</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">squares = [n**<span class="number">2</span> <span class="keyword">for</span> n <span class="keyword">in</span> range(<span class="number">10</span>) <span class="keyword">if</span> n%<span class="number">2</span>==<span class="number">0</span> ]</span><br><span class="line"></span><br><span class="line">squares = [</span><br><span class="line">    		n**<span class="number">2</span> </span><br><span class="line">    		<span class="keyword">for</span> n <span class="keyword">in</span> range(<span class="number">10</span>) </span><br><span class="line">    		<span class="keyword">if</span> n%<span class="number">2</span>==<span class="number">0</span> </span><br><span class="line">			]</span><br></pre></td></tr></table></figure>



</li>
</ul>
</li>
</ul>
<ul>
<li><p>functions</p>
<ul>
<li>len(list)</li>
<li>sorted(list)</li>
<li>sum(list)</li>
<li>max(list)   \   min(list)</li>
</ul>
</li>
<li><p>method   –&gt; help(list)  –&gt; learn all the method</p>
<ul>
<li>list.append()</li>
<li>list.pop()  : removes and returns the last element</li>
<li>searching<ul>
<li>“element” in list</li>
<li>list.index(‘element’)</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="tuple"><a href="#tuple" class="headerlink" title="tuple"></a>tuple</h3><ul>
<li>tuples(immutable)<ul>
<li>create<ul>
<li>t=(1, 2, 3)</li>
<li>t=1, 2, 3</li>
</ul>
</li>
<li>a, b = b, a</li>
</ul>
</li>
</ul>
<h3 id="loop"><a href="#loop" class="headerlink" title="loop"></a>loop</h3><ul>
<li>loops<ul>
<li>for planet in planets</li>
<li>for i in range(5)</li>
<li>while i&lt;10</li>
</ul>
</li>
</ul>
<h3 id="string"><a href="#string" class="headerlink" title="string"></a>string</h3><ul>
<li><p>Double quotes –&gt;  contains single quote charater</p>
</li>
<li><p>single quotes –&gt; contains double quote character</p>
</li>
<li><p>escaping –&gt; <code>\</code></p>
</li>
<li><p>string –&gt; sequnces of characters –&gt; list (immutable)</p>
<ul>
<li><p>index</p>
</li>
<li><p>slice</p>
</li>
<li><p>funcion</p>
</li>
<li><p>method for string</p>
<ul>
<li>.split()</li>
<li>.join()   <code>&#39;/&#39;.join([month, day, year])</code></li>
<li>str.format()</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">"&#123;&#125;, you'll always be the &#123;&#125;th planet to me."</span>.format(planet, position)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">s = <span class="string">"""Pluto's a &#123;0&#125;.</span></span><br><span class="line"><span class="string">No, it's a &#123;1&#125;.</span></span><br><span class="line"><span class="string">&#123;0&#125;!</span></span><br><span class="line"><span class="string">&#123;1&#125;!"""</span>.format(<span class="string">'planet'</span>, <span class="string">'dwarf planet'</span>)</span><br><span class="line"></span><br><span class="line"><span class="string">'&#123;:10.5&#125;'</span>.format(<span class="string">'xyz'</span>)    <span class="comment">#十个占位，5位有效数字</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h3 id="dictionaries"><a href="#dictionaries" class="headerlink" title="dictionaries"></a>dictionaries</h3><ul>
<li><p>create</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">numbers = &#123;<span class="string">'one'</span>:<span class="number">1</span>, <span class="string">'two'</span>:<span class="number">2</span>, <span class="string">'three'</span>:<span class="number">3</span>&#125;</span><br></pre></td></tr></table></figure>



</li>
</ul>
<ul>
<li><p>index  <code>index[&quot;one&quot;]</code></p>
</li>
<li><p>add <code>numbers[&quot;four&quot;]=4</code>]</p>
</li>
<li><p>change</p>
</li>
<li><p>dictionary comprehensions</p>
</li>
<li><p>‘one’ in numbers</p>
</li>
<li><p>遍历</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> k <span class="keyword">in</span> numbers:</span><br><span class="line">    print(<span class="string">"&#123;&#125; = &#123;&#125;"</span>.format(k,numbers[k]))</span><br><span class="line">    </span><br><span class="line"><span class="keyword">for</span> k,num <span class="keyword">in</span> numbers.items():</span><br><span class="line">    print(<span class="string">"&#123;&#125; = &#123;&#125;"</span>.format(k,num) )</span><br><span class="line">    <span class="comment"># dict.items() = dict.keys() + dict.values()</span></span><br></pre></td></tr></table></figure>






</li>
</ul>
<h3 id="external-libraries"><a href="#external-libraries" class="headerlink" title="external libraries"></a>external libraries</h3><ul>
<li><p>understanding strange objects</p>
<ul>
<li><p><code>type()</code>   –&gt;  what is it </p>
</li>
<li><p><code>dir()</code>  –&gt; what can be done  (模块中定义的模块，哈桑函数，变量)</p>
</li>
<li><p><code>help()</code>  –&gt; more info </p>
</li>
</ul>
</li>
<li><p><strong>operator overloading</strong></p>
</li>
</ul>
<h3 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h3><ul>
<li><p>cmd <code>python</code> -&gt; 进入到交互环境；<code>quit</code></p>
</li>
<li><p>注释 <code>#</code> <code>&#39;&#39;&#39;</code> <code>&quot;&quot;&quot;</code></p>
</li>
<li><p><code>end=&#39;&#39;</code>表示输出后不换行 （默认的结束符<code>\n</code>）</p>
</li>
<li><p>大小写敏感</p>
</li>
<li><table>
<thead>
<tr>
<th>函数</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>chr</code></td>
<td>整数-&gt;字符串</td>
</tr>
<tr>
<td><code>float</code></td>
<td>个字符串转换成浮点数</td>
</tr>
<tr>
<td><code>type</code></td>
<td>函数变量的类型进行检查</td>
</tr>
<tr>
<td><code>bin</code></td>
<td>整数-&gt;<code>&#39;0b&#39;</code>开头的二进制字符串</td>
</tr>
<tr>
<td><code>ord</code></td>
<td>字符串-&gt;整数</td>
</tr>
<tr>
<td><code>hex</code></td>
<td>整数-&gt;’0x’<code>开头的十六进制字符串，例如：</code>hex(123)<code>会返回</code>‘0x7b’`</td>
</tr>
<tr>
<td><code>input</code></td>
<td>从输入中读取一行，返回读到的字符串。</td>
</tr>
<tr>
<td><code>open</code></td>
<td>打开一个文件并返回文件对象</td>
</tr>
<tr>
<td><code>round</code></td>
<td>四舍五入</td>
</tr>
<tr>
<td><code>range(100, 0, -2)</code></td>
<td>100到1的偶数</td>
</tr>
<tr>
<td><code>/</code>  <code>//</code> <code>**</code></td>
<td>除法 整除  指数</td>
</tr>
<tr>
<td><code>print()</code></td>
<td>来实现换行输出</td>
</tr>
</tbody></table>
</li>
<li><table>
<thead>
<tr>
<th><code>is</code> <code>is not</code></th>
<th>身份运算符</th>
</tr>
</thead>
<tbody><tr>
<td><code>in</code> <code>not in</code></td>
<td>成员运算符</td>
</tr>
<tr>
<td><code>not</code> <code>or</code> <code>and</code></td>
<td>逻辑运算符</td>
</tr>
</tbody></table>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">f = float(input(<span class="string">'请输入华氏温度: '</span>))</span><br></pre></td></tr></table></figure>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(<span class="string">'%.1f华氏度 = %.1f摄氏度'</span> % (f, c))</span><br><span class="line">print(<span class="string">f'<span class="subst">&#123;f:<span class="number">.1</span>f&#125;</span>华氏度 = <span class="subst">&#123;c:<span class="number">.1</span>f&#125;</span>摄氏度'</span>)</span><br><span class="line">print(<span class="string">f'<span class="subst">&#123;i&#125;</span>*<span class="subst">&#123;j&#125;</span>=<span class="subst">&#123;i * j&#125;</span>'</span>, end=<span class="string">'\t'</span>)</span><br><span class="line">print()   <span class="comment">#来实现换行输出</span></span><br></pre></td></tr></table></figure>



</li>
</ul>
<ul>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ... :</span><br><span class="line"><span class="keyword">elif</span>:</span><br><span class="line"><span class="keyword">else</span>:</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>Flat is better than nested</strong></p>
</li>
<li><p><strong>for-in循环</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">1</span>, <span class="number">101</span>):   <span class="comment">#[1,101)</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>产生一个1-100范围的随机数</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> random</span><br><span class="line">answer = random.randint(<span class="number">1</span>, <span class="number">100</span>)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a><strong>函数</strong></h2>  <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fac</span><span class="params">(num)</span>:</span></span><br><span class="line">	....</span><br><span class="line">	<span class="keyword">return</span> result</span><br></pre></td></tr></table></figure>

<ul>
<li><p>函数也可以没有自变量，但是函数名后面的圆括号是必须有的</p>
</li>
<li><p><code>def roll_dice(n=2):</code> 默认值为2,如果没有指定参数,那么n使用默认值2;传入参数3，变量n被赋值为3</p>
</li>
<li><p>带默认值的参数必须放在不带默认值的参数之后</p>
</li>
<li><p><strong>可变参数</strong>: 可变参数其实就是将多个参数打包成了一个元组</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(*args)</span>:</span></span><br><span class="line">    total = <span class="number">0</span></span><br><span class="line">    <span class="comment"># 可变参数可以放在for循环中取出每个参数的值</span></span><br><span class="line">    <span class="keyword">for</span> val <span class="keyword">in</span> args:</span><br><span class="line">        total += val</span><br><span class="line">    <span class="keyword">return</span> total</span><br></pre></td></tr></table></figure>
</li>
<li><p>用模块管理函数-&gt;解决命名冲突</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> module1 <span class="keyword">as</span> m1</span><br><span class="line"><span class="keyword">import</span> module2 <span class="keyword">as</span> m2</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> module1 <span class="keyword">import</span> foo</span><br><span class="line"><span class="keyword">from</span> module1 <span class="keyword">import</span> foo <span class="comment">#导入同名函数，后导入覆盖先导入</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> module1 <span class="keyword">import</span> foo <span class="keyword">as</span> f1</span><br><span class="line"><span class="keyword">from</span> module2 <span class="keyword">import</span> foo <span class="keyword">as</span> f2</span><br></pre></td></tr></table></figure>



</li>
</ul>
<h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><h3 id="字符串-string-‘’"><a href="#字符串-string-‘’" class="headerlink" title="字符串(string ‘’)"></a>字符串(string ‘’)</h3><ul>
<li><p><strong>不可变类型</strong>-&gt;<strong>不能通过索引运算修改字符串中的字符</strong></p>
</li>
<li><p><code>\</code>（反斜杠）来表示转义</p>
<ul>
<li>\t    制表符</li>
<li>本身包含了<code>&#39;  \</code>这些特殊的字符，通过<code>\</code>进行转义处理</li>
</ul>
</li>
<li><p>原始字符串：以<code>r</code>或<code>R</code>开头 <code>s2 = r&#39;\time up \now&#39;</code></p>
</li>
<li><p><code>+</code>和<code>*</code> -&gt; 拼接和重复</p>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 比较字符串的内容</span></span><br><span class="line">print(s1 == s2, s2 == s3)    <span class="comment"># True True</span></span><br><span class="line"><span class="comment"># 比较字符串的内存地址</span></span><br><span class="line">print(s1 <span class="keyword">is</span> s2, s2 <span class="keyword">is</span> s3)    <span class="comment"># False True</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>成员运算</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(<span class="string">'wo'</span> <span class="keyword">in</span> s1)    <span class="comment"># True</span></span><br><span class="line">print(<span class="string">'wo'</span> <span class="keyword">not</span> <span class="keyword">in</span> s1)</span><br></pre></td></tr></table></figure>
</li>
<li><p>索引和切片</p>
<ul>
<li>正向索引 : 0<code>到</code>N-1</li>
<li>负向索引: -1<code>到</code>-N</li>
<li>切片: [i:j:k]</li>
</ul>
</li>
<li><p>循环遍历</p>
<ul>
<li><p>下标</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> index <span class="keyword">in</span> range(len(s1)):</span><br><span class="line">    print(s1[index])</span><br></pre></td></tr></table></figure>
</li>
<li><p>直接</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> ch <span class="keyword">in</span> s1:</span><br><span class="line">    print(ch)</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>查找</p>
<ul>
<li><p>find</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 找到了返回字符串中另一个字符串首字符的索引</span></span><br><span class="line">print(s.find(<span class="string">'or'</span>))        <span class="comment"># 8</span></span><br><span class="line"><span class="comment"># 找不到返回-1</span></span><br><span class="line">print(s.find(<span class="string">'shit'</span>))      <span class="comment"># -1</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>index</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 找到了返回字符串中另一个字符串首字符的索引</span></span><br><span class="line">print(s.index(<span class="string">'or'</span>))       <span class="comment"># 8</span></span><br><span class="line"><span class="comment"># 找不到引发异常</span></span><br><span class="line">print(s.index(<span class="string">'shit'</span>))     <span class="comment"># ValueError: substring not found</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>逆向查找:rfind<code>和</code>rindex</p>
</li>
</ul>
</li>
<li><p>格式化</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># center方法以宽度20将字符串居中并在两侧填充*</span></span><br><span class="line">print(s.center(<span class="number">20</span>, <span class="string">'*'</span>))  <span class="comment"># ****hello, world****</span></span><br><span class="line"><span class="comment"># rjust方法以宽度20将字符串右对齐并在左侧填充空格</span></span><br><span class="line">print(s.rjust(<span class="number">20</span>))        <span class="comment">#         hello, world</span></span><br><span class="line"><span class="comment"># ljust方法以宽度20将字符串左对齐并在右侧填充~</span></span><br><span class="line">print(s.ljust(<span class="number">20</span>, <span class="string">'~'</span>))   <span class="comment"># hello, world~~~~~~~~</span></span><br></pre></td></tr></table></figure>
</li>
<li><table>
<thead>
<tr>
<th>变量值</th>
<th>占位符</th>
<th>格式化结果</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>-1</code></td>
<td><code>{:+.2f}</code></td>
<td><code>&#39;-1.00&#39;</code></td>
<td>带符号保留小数点后两位</td>
</tr>
<tr>
<td><code>3.1415926</code></td>
<td><code>{:.0f}</code></td>
<td><code>&#39;3&#39;</code></td>
<td>不带小数</td>
</tr>
<tr>
<td><code>123</code></td>
<td><code>{:0&gt;10d}</code></td>
<td><code>0000000123</code></td>
<td>左边补<code>0</code>，补够10位</td>
</tr>
<tr>
<td><code>123</code></td>
<td><code>{:x&lt;10d}</code></td>
<td><code>123xxxxxxx</code></td>
<td>右边补<code>x</code> ，补够10位</td>
</tr>
<tr>
<td><code>123456789</code></td>
<td><code>{:,}</code></td>
<td><code>&#39;123,456,789&#39;</code></td>
<td>逗号分隔格式</td>
</tr>
<tr>
<td><code>0.123</code></td>
<td><code>{:.2%}</code></td>
<td><code>&#39;12.30%&#39;</code></td>
<td>百分比格式</td>
</tr>
<tr>
<td><code>123456789</code></td>
<td><code>{:.2e}</code></td>
<td><code>&#39;1.23e+08&#39;</code></td>
<td>科学计数法格式</td>
</tr>
</tbody></table>
</li>
<li><p><code>s.strip()</code> 修剪掉左右两端空格</p>
</li>
</ul>
<h3 id="列表-list"><a href="#列表-list" class="headerlink" title="列表(list [])"></a>列表(list [])</h3><ul>
<li><p>允许<strong>有重复</strong>的数据，可<strong>修改</strong>。列表底层是一个可以动态扩容的数组。</p>
</li>
<li><p>定义</p>
<ul>
<li><p>生成式 (最好)</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">items1 = [x <span class="keyword">for</span> x <span class="keyword">in</span> <span class="string">'hello world'</span> <span class="keyword">if</span> x <span class="keyword">not</span> <span class="keyword">in</span> <span class="string">' aeiou'</span>]</span><br><span class="line">items2 = [x + y <span class="keyword">for</span> x <span class="keyword">in</span> <span class="string">'ABC'</span> <span class="keyword">for</span> y <span class="keyword">in</span> <span class="string">'12'</span>]</span><br><span class="line">print(items3)    <span class="comment"># ['A1', 'A2', 'B1', 'B2', 'C1', 'C2']</span></span><br></pre></td></tr></table></figure>



</li>
</ul>
</li>
</ul>
<ul>
<li><p><code>[]</code>字面量语法</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">items1 = [<span class="number">35</span>, <span class="number">12</span>, <span class="number">99</span>, <span class="number">68</span>, <span class="number">55</span>, <span class="number">87</span>]</span><br></pre></td></tr></table></figure>
</li>
<li><p>内置的<code>list</code>函数</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">items1 = list(range(<span class="number">1</span>, <span class="number">10</span>))</span><br><span class="line">print(items1)    <span class="comment"># [1, 2, 3, 4, 5, 6, 7, 8, 9]</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<ul>
<li><p>拼接、重复、成员运算、索引,切片,比较</p>
<ul>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 列表的重复</span></span><br><span class="line">items4 = [<span class="string">'hello'</span>] * <span class="number">3</span></span><br><span class="line">print(items4)    <span class="comment"># ['hello', 'hello', 'hello']</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>添加和删除</p>
<ul>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用append方法在列表尾部添加元素</span></span><br><span class="line">items.append(<span class="string">'Swift'</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用insert方法在列表指定索引位置插入元素</span></span><br><span class="line">items.insert(<span class="number">2</span>, <span class="string">'SQL'</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 删除指定的元素</span></span><br><span class="line">items.remove(<span class="string">'Java'</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 删除指定索引位置的元素</span></span><br><span class="line">items.pop(<span class="number">0</span>)</span><br><span class="line"><span class="comment">#和pop基本相同，但会返回删除的元素</span></span><br><span class="line"><span class="keyword">del</span> items[<span class="number">1</span>]</span><br></pre></td></tr></table></figure>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">items.clear()</span><br><span class="line"><span class="comment"># 清空列表中的元素</span></span><br></pre></td></tr></table></figure>


</li>
</ul>
</li>
</ul>
<ul>
<li><p>元素位置和次数</p>
<ul>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查找元素的索引位置</span></span><br><span class="line">print(items.index(<span class="string">'Python'</span>))   </span><br><span class="line"><span class="comment">#从索引为3这个位置开始</span></span><br><span class="line">print(items.index(<span class="string">'Java'</span>, <span class="number">3</span>))</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li>  <figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(items.count(<span class="string">'Python'</span>))</span><br></pre></td></tr></table></figure>
</li>
<li><p>排序和反转</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">items.sort()</span><br><span class="line">items.reverse()</span><br></pre></td></tr></table></figure>
</li>
<li><p>嵌套的列表 -&gt; 表格或数学上的矩阵</p>
<ul>
<li><p>不能用<code>[[0] * 3] * 5]</code>这种方式来创建嵌套列表</p>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">scores = [[<span class="number">0</span>] * <span class="number">3</span> <span class="keyword">for</span> _ <span class="keyword">in</span> range(<span class="number">5</span>)]</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h3 id="元组–tuple"><a href="#元组–tuple" class="headerlink" title="元组–tuple ()"></a>元组–tuple ()</h3><ul>
<li><p>可重复，元组是<strong>不可变</strong>类型</p>
</li>
<li><p>定义</p>
<ul>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义一个四元组</span></span><br><span class="line">t2 = (<span class="string">'骆昊'</span>, <span class="number">40</span>, <span class="literal">True</span>, <span class="string">'四川成都'</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>()</code>表示空元组</p>
</li>
<li><p>只有一个元素，需要加逗号，否则是改变运算优先级的圆括号。<code>(&#39;hello&#39;, )</code>和<code>(100, )</code>才是一元组，而<code>(&#39;hello&#39;)</code>和<code>(100)</code>只是字符串和整数</p>
</li>
</ul>
</li>
<li><p>打包和解包 （解包语法对所有的序列都成立）</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = <span class="number">1</span>, <span class="number">10</span>, <span class="number">100</span> <span class="comment"># 打包</span></span><br><span class="line">i, j, k = a	<span class="comment"># 解包</span></span><br><span class="line"></span><br><span class="line">a = <span class="number">1</span>, <span class="number">10</span>, <span class="number">100</span>, <span class="number">1000</span></span><br><span class="line">i, j, *k = a  <span class="comment">#星号变量会变成列表</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>交换变量值</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a, b, c = b, c, a</span><br></pre></td></tr></table></figure>
</li>
<li><p>让函数返回多个值 -&gt; <code>return max_one, min_one</code>装成一个二元组然后返回,通过解包语法将二元组中的两个值分别赋给两个变量</p>
</li>
<li><p>元组和列表相互转换 <code>list()</code>  <code>tuple()</code></p>
</li>
<li><p><code>enumerate</code>  循环遍历取到一个二元组，解包之后第一个值是索引，第二个值是元素</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> index <span class="keyword">in</span> range(len(items)):</span><br><span class="line">    print(<span class="string">f'<span class="subst">&#123;index&#125;</span>: <span class="subst">&#123;items[index]&#125;</span>'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> index, item <span class="keyword">in</span> enumerate(items):</span><br><span class="line">    print(<span class="string">f'<span class="subst">&#123;index&#125;</span>: <span class="subst">&#123;item&#125;</span>'</span>)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="集合-set"><a href="#集合-set" class="headerlink" title="集合(set {})"></a>集合(set {})</h3><ul>
<li><p>无序性-&gt;不能索引 ； 互异性-&gt; 去重 ； 确定性：<code>(not) in</code>成员运算</p>
</li>
<li><p>集合中的元素必须是<code>hashable</code>类型,通常不可变类型都是<code>hashable</code>类型 (集合本身也是可变类型)。集合底层使用了<strong>哈希存储</strong>的方式</p>
</li>
<li><p>不可变类型的集合，名字叫<code>frozenset</code>,可以作为<code>set</code>中的元素</p>
</li>
<li><p>创建</p>
<ul>
<li><p><code>{}</code>字面量语法 (至少有一个元素,否则是空字典)</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">set1 = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">3</span>, <span class="number">3</span>, <span class="number">2</span>&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>构造器语法<code>set</code> (空集合可以使用<code>set()</code>)</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">set2 = set(<span class="string">'hello'</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p>生成式（同list）</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">set4 = &#123;num <span class="keyword">for</span> num <span class="keyword">in</span> range(<span class="number">1</span>, <span class="number">20</span>) <span class="keyword">if</span> num % <span class="number">3</span> == <span class="number">0</span> <span class="keyword">or</span> num % <span class="number">5</span> == <span class="number">0</span>&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>运算</p>
<ul>
<li><code>in</code>和<code>not in</code></li>
<li>交并差 (&amp;|-)</li>
</ul>
</li>
<li><p>添加或删除</p>
<ul>
<li><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set1.add(33)</span><br></pre></td></tr></table></figure>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">set1.discard(<span class="number">100</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#建议先做成员运算再删除,因为不在集合中会引发KeyError异常</span></span><br><span class="line"><span class="keyword">if</span> <span class="number">10</span> <span class="keyword">in</span> set1:</span><br><span class="line">    set1.remove(<span class="number">10</span>)</span><br><span class="line">print(set1)    <span class="comment"># &#123;33, 1, 55, 1000&#125;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># pop-&gt;随机删除一个元素并返回该元素</span></span><br><span class="line">print(set1.pop())</span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># clear方法可以清空整个集合</span></span><br><span class="line">set1.clear()</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>判断有无相同的元素可以使用<code>print(set1.isdisjoint(set2))</code></p>
</li>
</ul>
<h3 id="字典（zip-）"><a href="#字典（zip-）" class="headerlink" title="字典（zip{}）"></a>字典（zip{}）</h3><ul>
<li><p><strong>字典中的键必须是不可变类型</strong> (列表，集合，字典不行，但是他们可以为键值)</p>
</li>
<li><p>定义</p>
<ul>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">person = &#123;<span class="string">'name'</span>: <span class="string">'王大锤'</span>,<span class="string">'age'</span>: <span class="number">55</span>,<span class="string">'weight'</span>: <span class="number">60</span>, &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>内置函数<code>dict</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">person = dict(name=<span class="string">'王大锤'</span>,age=<span class="number">55</span>,weight=<span class="number">60</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 可以通过Python内置函数zip压缩两个序列并创建字典</span></span><br><span class="line">items1 = dict(zip(<span class="string">'ABCDE'</span>, <span class="string">'12345'</span>))</span><br><span class="line">print(items1)    <span class="comment"># &#123;'A': '1', 'B': '2', 'C': '3', 'D': '4', 'E': '5'&#125;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>生成式语法</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">items3 = &#123;x: x ** <span class="number">3</span> <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">1</span>, <span class="number">6</span>)&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>修改</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="string">'age'</span> <span class="keyword">in</span> person:</span><br><span class="line">    person[<span class="string">'age'</span>] = <span class="number">25</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>方法</p>
<ul>
<li><p>通过键获得值</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 取不到返回None或设定的默认值</span></span><br><span class="line">print(students.get(<span class="number">1005</span>))    <span class="comment"># None</span></span><br><span class="line">print(students.get(<span class="number">1005</span>, &#123;<span class="string">'name'</span>: <span class="string">'无名氏'</span>&#125;))    <span class="comment"># &#123;'name': '无名氏'&#125;</span></span><br></pre></td></tr></table></figure>
</li>
<li><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 获取字典中所有的键</span></span><br><span class="line">print(students.keys())      <span class="comment"># dict_keys([1001, 1002, 1003])</span></span><br><span class="line"><span class="comment"># 获取字典中所有的值</span></span><br><span class="line">print(students.values())    <span class="comment"># dict_values([&#123;...&#125;, &#123;...&#125;, &#123;...&#125;])</span></span><br><span class="line"><span class="comment"># 获取字典中所有的键值对</span></span><br><span class="line">print(students.items())</span><br></pre></td></tr></table></figure>
</li>
<li><p>删除</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用pop方法通过键删除键值,并返回该值</span></span><br><span class="line">stu1 = students.pop(<span class="number">1002</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用popitem方法删除字典中最后一组键值,返回对应的二元组</span></span><br><span class="line"><span class="comment"># 如果字典中没有元素，调用该方法将引发KeyError异常</span></span><br><span class="line">key, value = students.popitem()</span><br><span class="line"></span><br><span class="line"><span class="comment">#del,引不到对应的值，一样会引发KeyError异常</span></span><br><span class="line">person = &#123;<span class="string">'name'</span>: <span class="string">'王大锤'</span>, <span class="string">'age'</span>: <span class="number">25</span>&#125;</span><br><span class="line"><span class="keyword">del</span> person[<span class="string">'age'</span>]</span><br></pre></td></tr></table></figure>
</li>
<li><p>更新</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#setdefault可以更新字典中的键对应的值或向字典中存入新的键值对</span></span><br><span class="line">result = students.setdefault(<span class="number">1005</span>, &#123;<span class="string">'name'</span>: <span class="string">'方启鹤'</span>, <span class="string">'sex'</span>: <span class="literal">True</span>&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用update更新字典元素，相同的键会用新值覆盖掉旧值，不同的键会添加到字典中</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>python</category>
      </categories>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
</search>
